# Project Intent Notes

## Authorized Color Repair Release — 2026-09-07

- 用户在本地修复提交 cb17c35 后要求“更新”，按已确认的双端发布约定执行 alpha-0.1.2 / 0.1.2.0：提交推送、Windows 自带依赖 EXE 和同源 Web 更新部署，服务器使用 GitHub pull。保留全部旧产物和更新历史。
- 本次发布不能将默认色故障注入通过写成已复现真实设备触发条件。正式产物需补验颜色交错回归，Web 另需异常默认色注入回归及原始/gzip、有/无隔离验收。部署与产物检查完成前不声称交付成功。

## Black Sprite Follow-Up — 2026-09-07

- 用户提供 alpha-0.1.1 实际游玩 F3 截图，敌人与部分弹幕、掉落物呈黑色轮廓；不能继续将此前边界裁剪修复视为发黑也已解决。截图 Seed 7211169、Tick 2821、47.02 秒，Web Compatibility，逻辑 1280×720。种子不能代替完整输入记录。
- 本轮按日常修复处理：保持 Windows/Web 同一 C# 绘制实现，不改玩法、素材或实体数量；不升版、不推送、不导出正式 EXE、不部署，完成后本地提交。
- 排查确认原批次 QuadMesh 没有顶点颜色输入；故障注入将默认顶点色置黑、染色或透明后旧实现失败，显式白色顶点 ArrayMesh 后通过。普通本机绘制未自然复现；此证据证明默认色依赖已去除，不证明用户设备的具体驱动触发原因已经确定。旧 AABB/UV 修复、实例颜色与透明度全部保留。

## Authorized Sprite Repair Release

- alpha-0.1.1 已双端发布：同源提交 71e3220，Windows 193,121,856 字节、自带依赖，22 项独立检查通过；Web 四组完整流程、动态贴图回归、下载异常及公网三入口验收通过。实际线上目录 alpha-0.1.1-71e3220-20260907T040525Z，旧版保留，详情和校验值见 docs/deployment.md。

- 2026-09-07 用户明确要求发布：本轮将已提交的共用贴图修复发布为 alpha-0.1.1，Windows 数字版本 0.1.1.0；授权提交、推送、同源 EXE 导出与 Web 更新部署。保留 alpha-0.1.0 和全部旧产物，下方日常修复记录为历史。
- 正式产物必须补验本次动态贴图回归，不能只沿用静态截图。发黑未单独复现和小米真机未验收的限制继续保留。

## Shared Sprite Visibility Repair

- 用户报告 alpha-0.1.0 在 Windows 与浏览器游玩途中实体贴图消失或发黑。本轮只修复并验证共用绘制，不改玩法、素材来源或操作，不自动升版、导出正式 EXE、推送或部署。
- 已在固定种子长局约 144 秒复现敌人贴图消失：CPU/原生实例、贴图及颜色数据仍有效，普通绘制能显示，批次绘制缺失 1461 个像素。显式提交当前紧致边界后同场景差异归零；另修正 QuadMesh 纵向 UV 与普通精灵不一致的问题。发黑未单独复现，不将局部根因当作所有症状都已解决。
- 最终复验：Windows 与无隔离、无共享内存的 Web，各完成灵梦／魔理沙 144 项精灵像素对照及 24 处完整战斗对照，均通关且对照无差异；核心 29 项、UI、五项桌面渲染及 Web DPR 1/3 检查通过。正式 alpha-0.1.0 EXE 和线上部署未改动，修复记录进入未发布日志。

## Authorized Compatible Dual-Target Publication

- alpha-0.1.0 已双端交付：同源提交 07b96e7，Windows 自带依赖单 EXE 与服务器单线程 Web，旧版和候选保留。Windows 20 项、Web 四组共 20 项流程、20 次原始下载、六项下载场景及公网三种入口通过；无隔离入口确实没有 SharedArrayBuffer。验收脚本修正引擎等待状态分类并保留原始失败证据，未更换游戏产物。网络首次加载仍可能需要较长时间；小米真机和手机帧率尚未验收。详见 docs/deployment.md。

- 用户确认现在正式发布：本地导出自带依赖 Windows EXE，并更新域名下的 Web 游戏；允许更新版本号、更新记录、Git 提交与推送、服务器 pull 和部署。
- 2026-09-07 用户确认双端正式发布改为 alpha-0.1.0，Windows 数字版本为 0.1.0.0；提升发布序号、优化序号归零，不擅自改成 stable。保留所有旧版 EXE 及 alpha-0.0.10 未发布候选，后者不作为线上历史发布。两端必须来自同一游戏源码提交，Web 使用不要求共享内存的单线程兼容构建。
- 正式上线前验证同一产物的原始与 gzip 路径、有隔离与无隔离环境；上线后复测公开资源与触屏。测试模拟不代替小米真机验收，不承诺手机帧率，不增加 Android/iOS 打包。

## Unified Platform Toolchain Authorization

- 兼容验证已通过有隔离/无隔离的十项浏览器回归，未改动 game 下的 C# 玩法；Windows 编译、29 项核心与真实渲染回归通过。旧 UI 总验收仍受当前工作区发布日志断言影响，不能称全部测试通过。小米真机、原生移动端与线上部署尚未验收。
- 用户授权调整 Web 导出工具链以验证真正的单线程兼容版；最终 Windows 与 Web 必须仍是一个项目、一套 C# 游戏，不得分叉维护两套玩法。平台差异只进入构建配置及必要的平台接口。
- 用户提出未来也可开放移动端，因为已有触屏操作；当前仅保留这一扩展方向，不擅自增加 Android/iOS 打包或宣称触屏等于原生移动端适配完成。
- 本轮兼容研究与仓库内已有 alpha-0.0.10 发布工作分开：保留现有发布改动，使用隔离缓存，不修改正在发布的产物或服务器。详细验证计划记录在 docs/threadless-web.md。

## Authorized Dual-Target Release

- 用户明确要求发布：沿用版本口径，将当前已实现改造发布为 alpha-0.0.10；交付新的自带依赖 Windows EXE 与对应网页部署，提交并推送 Git，保留旧发行产物与日志。
- 本次发布不等于已实现单线程 C# Web 兼容版。无跨源隔离或 SharedArrayBuffer 的浏览器仍不能运行现有工具链，必须在发布说明中明确保留该限制，不声称小米浏览器问题已修复。
- 发布验证不能通过忽略 ERR_ABORTED 或修改测试报告来绕过门禁；应覆盖实际部署的 gzip 资源与加载清单，并保留原始资源路径的独立诊断证据。

## Web Compatibility Without Shared Memory

- 用户补充失败环境为小米自带浏览器，并明确要求适配无法启用跨源隔离或共享内存的情况；不能把要求用户换浏览器当作兼容目标已经完成。不能据此断言所有小米浏览器版本均不支持，具体系统与浏览器版本仍未知。
- 兼容目标是在保留同一项目与 C# 玩法的前提下提供真正不依赖 SharedArrayBuffer 的 Web 运行路径。服务器负责正确分发与响应头，客户端按能力而不是浏览器品牌选择已验证的构建；不允许只关闭检测、伪造 SharedArrayBuffer 或移除现有隔离头来掩盖运行时要求。
- 当前检查仅确认工具链限制与目标，未实现单线程可运行产物：线上入口返回 COOP same-origin / COEP require-corp；现有 Web 导出选项、配套 Browser.targets 与实际 JS 产物均启用线程或共享内存。需要成套调整 Web 模板、.NET 运行时与 SDK，不能只改 Caddy。Windows 产物和线上配置本轮未改动。

## Mobile Performance And Asset Priority

- 本轮已完成轻量组件/系统战斗运行时、批量绘制、原作素材和新图标的本地改造；不是通用全 SoA ECS 框架。29 项核心与桌面界面验证通过，真机性能仍需设备复测。严格 Web 回归存在完整收包后的 ERR_ABORTED 事件，纯下载流页面也能复现且资源哈希一致；未忽略错误、未宣称网页验收全通过，生产加载器不保留无效的试验修复。
- 用户实测反馈手机约 9 FPS、平板约 25 FPS，要求检查并落实 OOP/ECS 混合架构；不能把架构名词当作性能保证，应测量模拟、渲染、分辨率和分配成本。设备型号、浏览器和具体战斗负载尚未提供，不能承诺优化后的真机帧率。
- 当前任务优先使用已有原作素材包中的素材，并更换游戏图标；保留素材来源与必要的使用范围说明，不擅自以新生成素材替代可用原作素材。共享 C#、东方加武侠加幸存者、人物原设能力、旧版操作和双端发布规则继续保留。
- 先做日常开发、验证和 Git 提交，不自动推送、部署或导出发行 EXE。不得为了性能暗改伤害、刷新数量或删掉战斗玩法。


## Physical Mobile Compatibility Report

- 用户提供手机浏览器截图：线上旧加载页报告 Cross-Origin Isolation 与 SharedArrayBuffer 不满足运行条件。这是真实失败报告，不能用桌面触控模拟通过替代手机兼容验收，也不能仅凭浏览器界面猜测品牌或内核版本。
- 本轮只读复查：HTTP/HTTPS、带/不带尾斜杠及 www 入口最终均返回带 COOP same-origin / COEP require-corp 的 HTTPS 页面。真实线上 Edge 152.0.4191.62 桌面与移动布局模拟均为 secureContext=true、crossOriginIsolated=true、SharedArrayBuffer=function、missingFeatures=[]；检查时主动阻止游戏包下载，仅验证能力，不算开局验收。
- 因当前访问路径未复现缺失隔离头，仍需用户提供手机系统和浏览器名称、版本（可用“关于浏览器”截图）才能区分浏览器能力限制、缓存或用户网络中间环节。不得将问题断言为服务器已修复或手机一定不支持；本轮未修改线上配置或发布。

## Web Loading Experience

- 用户要求首次加载明确显示总资源量、已接收量、实时速度及进度，不能只放没有说明的进度条。下载完成后的解压、编译和初始化必须单独说明；网络停顿不得制造假进度。
- 计量必须区分 gzip 传输正文与解压后的资源大小，不把约 98 MB 的资源处理量冒充约 32 MB 的网络下载量；缓存命中时明确接收/读取速度不代表网络带宽。
- 本次加载体验修改按日常开发处理：本地构建与验证、记录未发布日志并提交 Git，不自动部署、不升版本号、不导出新 EXE。后续明确发布时仍须同时交付 EXE 与网页更新。
- 最终本地验收：5 项计量测试、7 项部署保护测试、6 组真实资源加载场景和 5 组完整游戏回归通过，包含停顿恢复、错误重试与缓存再访问；桌面、横屏和竖屏加载截图已检查。当前验证构建核心压缩正文为 32.78 MB，不含页面和脚本等少量开销；线上仍是此前部署，不能说新加载页已上线。

## Deployment And Release Contract

- 首次公网试部署完成，稳定入口已可访问；实际启用的是 `alpha-0.0.9-f605aa0-20260906T164408Z`（目录时间为 UTC），来源提交 `f605aa0`。真实 HTTPS、电脑正常开局、触控模拟、刷新保存和竖屏暂停通过；根站点与原有路由状态不变。旧 EXE 保持原哈希，不能把这次网页试部署说成已经发布新 EXE。

- 用户确认目标为 `https://allinagent.top/TouhouSurvivor/`，服务器 `170.106.119.27`，使用用户提供的 `ljy.pem`。实际可登录用户为 `ubuntu`，已有 Caddy 站点；部署不能影响根站点、`/tusharedata/` 或已有拒绝路由。密钥内容不得进入仓库、日志或服务器上传包。
- 用户明确授权本轮推送 GitHub，并要求服务器首次 `git clone`、后续 `git pull`（语音输入的 hit pull 按上下文理解为 git pull）。服务器同步同一仓库的源码与部署脚本；已验证的 Web 构建产物单独传输，不将构建缓存、密钥或近 100 MB 的生成文件提交 Git。后续拉取使用 `--ff-only`，不覆盖服务器手工修改。
- 从现在起，“发布”必须包含两项交付：1. 对应版本的自带依赖 Windows EXE；2. 同一版本的 Web 站点更新部署，并完成线上检查。只完成其中一项必须说明部分完成，不能称为整版发布成功。
- 当前请求是首次试部署，不自动等同于要求提升版本号或重新导出 Windows 发行版。日常提交、Git 推送与发布仍分开；未明确要求时不推送 Git。部署前保留现有站点与回滚路径，不自动删除历史版本。

## Active Direction (2026-09-06)

- 共享项目本轮验收：Windows 26/26 核心、界面、旧档与完整显示设置通过；Edge 五组 Web 测试通过，包含刷新保存、三指触控、旋转暂停、升级与灵梦/魔理沙完整 Boss 至胜利流程。仍未进行 Android/iPhone 实机、真实网络首包、主观音效或素材发行授权验收；不能据此直接公开上线。

- 同项目双目标已落实：Windows 继续正式 4.7.1/.NET 8；Web 在自动生成构建目录选择社区实验 4.6.1/.NET 9，不能用实验编辑器手工维护另一份玩法。源码与字体、输入、存档接口共享；浏览器与 Windows 的记录格式兼容，但不会跨设备自动同步。

- 用户确认继续优化为同一个项目，同时支持 Web 与自带依赖 Windows EXE；保留同一套 C# 玩法/存档结构/素材，不维护两份游戏源码。平台差异限于输入、持久化、显示与构建；允许构建脚本自动生成隔离中间目录，不能把中间目录作为第二项目手工迭代。
- 本轮是日常开发，不是发布：不更新版本号、不导出新的 Windows 发行 EXE、不推送或上传服务器。Web 测试产物用于本地验证，不等于已完成真机或公网验收。

- 用户希望部署在现有域名的 /TouhouSurvivor/，电脑与手机浏览器直接游玩，并明确偏好继续使用 C#；本轮只授权先验证 Godot C# 实验性 Web 路线，不等于同意改语言、换引擎或上线服务器。
- 验证在隔离副本与工具链中进行，保留 Windows alpha-0.0.9 和 .NET 8 发布环境；不运行修改全局配置的实验安装脚本，不改正式版本号、不发布、不推送。手机模拟不能写成真实手机通过。
- 本轮 C# Web 实测：真实游戏可以导出并在部分浏览器运行；4.7.1 社区模板在当前 Edge 有渲染故障，4.6.1 仅作为隔离对照而非已批准的降级。字体、JSON 裁剪存档、手机控制与真机验收仍未完成，不能上线；详细证据在 docs/csharp_web_probe.md。

- 用户明确要求“发布一版”：本轮将已经提交的完整设置、像素 UI 与 F3 合并交付为 alpha-0.0.9，自带依赖单 EXE，更新完整日志并保留旧成品；此请求不包含远端推送。
- 已交付 alpha-0.0.9：192,542,872 字节，自带 Microsoft.NETCore.App 8.0.6，SHA-256 为 E21BFEDEC06B3DD1F2E730A97776C5B14BF609F7361FC73D4854509C42043AE8。源码 26/26 核心回归及完整 UI/设置/F3 检查通过，单 EXE 二十项隔离检查通过；不代表干净 Windows VM 或真人手感验收。

- 用户要求 UI 更美观，尝试像素风并参考《东方夜雀食堂》；参考视觉语言，不直接移植其界面贴图或作品身份。本轮优先统一面板、控件与菜单氛围，不改战斗规则。
- 用户追加恢复 F3，参考 Minecraft：制作可开关、可改键的只读诊断信息层，显示真实性能、位置和本局状态；不由此引入作弊功能、地图恢复或虚构指标。

- 用户明确区分提交、推送、发布：日常改动完成验证后提交 Git；未经要求不推送。只有用户明确要求发布时才更新版本号、生成新版本 EXE；日常更新写入未发布记录，不触碰历史发行成品。

- 用户授权继续优化并交付新版本。本轮 alpha-0.0.8 将人物设定纠偏落实到角色独立能力池、攻击机制、符卡表现与文案；保留 E 构筑、暂停返回、音量及历史日志，验证后导出新 EXE，不覆盖旧版。
- 用户明确校正题材边界：武侠应是风格和内核，不是给东方人物强加御剑、雷法等表层标签。必须尊重角色身份、能力与符卡依据；灵梦的巫女、符纸/阴阳玉等表达，以及魔理沙的星光、魔法/魔炮表达，不应被统一武器池抹平。后续角色先核对 Wiki 所引原作资料，再做机制转译，不能只改技能名字。
- 用户常用语音输入，会出现谐音字与漏字。应结合东方语境自然纠正，如符卡/符纸、御剑、雾雨魔理沙；不逐字曲解、不反复要求确认显然的错字，只有实质影响需求的歧义才询问。
- 用户随后以「做吧」授权实施。首轮聚焦操作与信息可读性：统一暂停/返回、构筑属性查看、基础音量设置、游戏内历史更新日志，并验证后交付下一版独立 EXE；不自动扩张为全量旧功能恢复。
- 用户进一步澄清「推倒重来」：旧版是体验参考，不是必须逐项恢复的功能清单。渐进补回时，每项都要重新判断是否值得保留、解决什么玩家问题，以及有没有更好的实现方式；不能把重写变成机械复刻旧版。
- 渐进贴近旧版操作，重点是尊重熟悉的操作习惯、降低不必要的重新学习成本，不代表旧界面、旧系统或旧架构不可改变。新版新增机制同样需要接受价值审视，不能仅因已经实现就默认保留。
- 当前讨论只确认上述决策原则；具体功能的保留、重做、暂缓优先级仍属建议，不视为用户已批准的实施清单。本轮不改玩法、不导出新版本。
- 用户当前要求两项评估：网页端部署可行性，以及旧版与新版的真实功能增减对比。此轮只做证据核对与分析，不据此擅自迁移语言、恢复玩法或部署服务器。
- 用户再次明确：每个版本必须有更新记录，保留此前版本日志原文，并追加当前版本的新增、变更、删减与限制；不能用重写设计文档取代 CHANGELOG.md。本轮补全 alpha-0.0.6 记录，并在成品目录附带同一份日志，不覆盖既有 EXE。

- 产品方向保持「东方 + 武侠 + 幸存者」；用户随后明确人物身份与能力设定不能被表面武侠标签覆盖，详见当前人物纠偏记录。设计文档、架构与玩法可重新设计，以好玩为优先；更早的细节限制仅作历史参考，不覆盖这些新确认。
- 当前重写采用独立 `game/` 运行时，只编译新实现；旧 `src/`、旧测试与素材保留原位，不删除文件、不迁就旧架构、不推送远端。
- 新权威设计为 `docs/rebirth_design.md`；下方旧笔记和旧设计文档仅作为历史参考，不代表新版本已实现的功能。
- 首次交付聚焦一张可探索的战场、两名差异化角色、自动战斗、擦弹蓄势、闪身、三选一构筑、精英与终局 Boss、结算重开。
- 当前显示名为「幻想乡 · 夜境异闻」，版本号延续既有口径为 `alpha-0.0.8`；新入口为 `game/Main.tscn`。内部 application/config/name 保留原字符串以稳定旧存档路径，窗口使用新显示名，不能因题名调整丢失设置与成绩。
- 用户明确要求交付导出的自带依赖 Windows EXE，而不是源码启动脚本。命名沿用 `TouhouWuxiaSurvivor_<stage>-<major>.<release>.<optimization>.exe`；阶段只允许 alpha/beta/rc/stable，Windows 数字版本映射为 major.release.optimization.0。不得自行使用 rebirth 等新阶段前缀。
- 本次延续既有 `alpha-0.0.0` 至 `alpha-0.0.5` 序列，不自行升阶段或发布序号；保留全部历史成品，验证 PCK 与 .NET 构建输出内嵌后的单文件运行。
- 已交付 `release/TouhouWuxiaSurvivor_alpha-0.0.6.exe`（192,443,224 字节）；仅复制此 EXE 到项目外空目录后，界面流程、存档测试、实际标题与 Boss 渲染均通过。后续交付继续以此类成品为准。
- 已交付首轮渐进改进 `release/TouhouWuxiaSurvivor_alpha-0.0.7.exe`（192,466,816 字节）：E 构筑查看、Esc/P 及子页返回、当次升级说明、音量滑块、按版本浏览内嵌历史日志。20 项核心回归与单 EXE 六项隔离检查通过；旧版本原文与成品保留，本轮没有改战斗数值或恢复大规模旧系统。
- 已交付人物纠偏版 `release/TouhouWuxiaSurvivor_alpha-0.0.8.exe`（192,484,304 字节）：两人各三条专属能力、不同符卡效果、角色归属双层校验与出处说明。26 项核心测试、六场正常自动导航通关及 13 项独立 EXE 检查通过；不代表真人可玩性验收。保留所有旧成品和日志，不推送远端。
- 新版不声称迁移旧版全功能，也不声称已通过真人可玩性验收；本次交付是独立重写且具有胜败闭环的内部试玩版。
- 现有原作素材仅复用作内部原型验证，不能据此声称具备公开发行授权。

## Clean Rewrite Request (2026-09-06)

- 用户要求先将当前项目保存到 Git，再完全从零重构，而不是继续适配旧实现。
- 旧实现快照：`d229b36eb1f84dd2758fcb7bac400f9d48b9f1fa`；仅本地提交，未推送远端。
- 快照覆盖仓库内未被忽略的文件；父目录素材包、Git 忽略的缓存、构建产物和日志仍保留原位，未纳入该提交。
- 前置确认时曾暂停重写以询问约束；用户随后确认仅三项题材方向不变，详见上方 Active Direction。
- 本次未删除旧文件；保留素材、旧代码和 Git 历史，从零重建独立实现与测试，设计文档可重新定义。

本文件记录已经确认的产品意图与工程边界；新实现应优先遵守这些定义，出现冲突时再与用户确认。

## Core Experience

- 游戏是东方 Project 同人、武侠风、像素表现的类幸存者游戏，使用 Godot C#。
- 项目采用“一切皆插件”的工程基调：内核、玩法规则和内容通过稳定模块契约组合，但玩家侧统一称为“内容包”，不称 DLC。
- 玩法规则由必选规则模块统一提供；内容包只能增加敌人、结构、角色、Build、奥义和表现，不能修改经验、压力、槽位、输入或胜负规则。
- 幻想乡本体是始终启用的基础内容包，必须独立形成完整游戏；东方红魔乡是第一份可关闭的参考内容包，用来验证完整内容契约。
- 当前优先级固定为先完成 Base-only，再完成 Base+TH06；其他作品保留资料但暂停新增玩法，不能用清单数量代替运行时完成度。
- 玩家局内只负责移动、走位和构筑选择；不增加需要主动按键施放的战斗技能。
- 奥义允许独立定时或战况被动触发，但不消耗灵力或其他充能资源。
- 本体必须形成完整闭环；每个作品内容包只横向增加地区、结构、敌人、角色与符卡选择，不提供纵向付费强度。
- 可选内容包彼此平行，不得依赖另一个可选内容包，也不得修改全局经验、压力换档、槽位容量或基础属性公式。
- 第一阶段内容包只携带声明式数据与素材，不执行任意 C# DLL；内部通用机制可以插件化，但玩家安装的是内容包而不是规则插件。
- 内容包通过稳定 ASCII ID、schema、内容版本和 host API 注册；中文名与译名只作显示，不能参与存档、引用、互斥或 Boss 排除。
- 若内容包需要新机制，必须先将其实现为所有内容都可使用、带数值预算和测试的通用能力，再由内容定义引用；通用系统禁止判断具体作品 ID。
- 已安装、已启用与本局激活是三个不同状态；开局后必须冻结规则版本、内容包版本与指纹、角色和世界种子，运行中不热加载或替换内容。
- 当前清单身份契约为 `schema_version = 1`、`host_api = content-v1`；Base 与 TH06 内容版本为 `0.1.0`，其余迁移库存为 `0.0.0`，只有显式能力声明可以启用对应通用机制。
- 图鉴读取已安装内容，本局世界、敌人、Boss 与升级池只读取冻结后的激活内容；禁用内容包后不得有任何运行时身份或素材泄漏。
- 图鉴固定覆盖地区、结构、敌人、角色、武学、符卡六类，并从正式运行目录投影数值、AI、前置、弹型姿态和素材来源；不得维护会落后于玩法的平行静态说明。
- 图鉴素材说明必须区分本包原生、带原因与审核状态的跨作代理、中文回退；奥义页展示的弹型和姿态规则必须与正式弹丸解析器同源。
- 局外存档中的缺包数据保留休眠；进行中的一局缺少原内容包时应明确拒绝续局并列出缺项，不能静默使用本体内容替代。
- 当前标准单局目标约五分钟；具体换档由玩家实际清场能力决定，击破最终角色 Boss 后成功结算，也可以保留构筑进入无尽游历。
- 每张已悟奥义都在底部 HUD 右上方显示独立文字印章；冷却遮罩从下边缘向上收缩，就绪后完全退去，被动条件尚未满足时显示待机边框。
- 自动射击必须读取目标运动速度并按实际弹速预判拦截；寿命内没有可靠交点时才回退直瞄。
- 有限流程的阶段推进只比较最近三十秒滑动窗口中的累计击破数 K 与实际刷新数 S；观察满三十秒且 `K >= ceil(0.90 * S)`、`S > 0` 才提高一档，失败后窗口继续逐帧滑动，不按互不相交的固定窗口结算。
- 不设置敌人存活软上限、换档超时或自动清场；总刷新率必须逐档严格上升，当前八档为 `2.40/3.15/4.05/5.10/6.15/7.20/8.25/9.30` 每秒。
- 当前刷新率是在上轮翻倍结果上再乘 `1.5`；只增加敌群供给，不改变各档强敌占比、怪物基础属性或 `K/S` 换档条件。
- 玩家选择无尽时记录实际进入时刻；之后每经过名义 30 秒，总刷新率在 9.30/s 基础上增加 0.12/s，最终配比不变且怪物种类属性仍固定。
- 敌群从 `100/0/0/0` 的普通敌人起步，最终保持 `40/30/22/8` 的普通/强敌/精锐/头目配比；难度主要通过更多敌人增长，强敌占比平缓混入。
- Boss 阶段继续使用最近目标和普通碰撞规则；不得为 Boss 增加专用穿透、保留弹或固定火力配额，接近 Boss 的通路由割草输出自然清开。
- 角色 Boss 基础生命统一乘 `6.0`，接触伤害与移动速度不额外提升；第一轮仅本体与红魔乡 Boss 使用自己所属符卡的图集、几何和阶段演出，其他作品保持通用弹幕。
- 本体保留六张横向奥义；红魔乡七名登记角色各有一张代表符卡，当前全目录共五十一张。后续作品按同一数据契约逐作补齐，不改变槽位容量。
- 怪物生命、移动、接触伤害、掉落和弹丸属性只由怪物种类决定，不随时间或压力档位增长；未来增幅怪必须作为明确的新种类或修饰类型设计。
- 普通弹始终围绕最近目标的预测交点发射；自机中心弹幕允许向外辐射或形成二至四重螺旋，负责近身割草而不参与锁敌。
- 每张奥义必须声明独立 `bullet_style` 美术语义；轨迹几何只管飞行与阵形，不能再用 `orbit/fan/line/ring/backstab` 猜弹丸外形。
- 原作 BulletAtlas 按 TH06、TH07-09、TH10-19 三套真实版式切片；刀、蝶、火焰、大玉等 32 像素帧必须整帧读取，禁止统一按 16 像素网格切碎。
- 普通自瞄弹使用针形，中心弹幕使用星形，敌弹在玉、碎晶、针、星之间轮换；它们共享切片目录但不共享错误的单一阴阳玉区域。
- 针、刀、光束、碎晶等方向型弹丸必须从实时速度推导姿态；追踪转向同步转身，阵式沿径向排布，对称弹不做无语义自转。
- 素材身份属于内容包：必须先取本局实际内容包的原生同语义素材；只有该包缺少同语义素材时，才允许使用在来源、原因和审核状态中明确登记的跨作代理，通用系统不得静默借用本体或其他作品素材。
- 符卡必须以原作游戏截图与可核对 Wiki 编号为依据；伤害效果、空间几何、弹型轮廓和跨帧演出是四个独立维度，冻结、时停转向、五行轮换、曳尾和扫掠等时序不得压成通用追踪或瞬时圆伤。
- 声明 `boss_spell_sequences` 能力的内容包，其每张符卡都必须提供原作编号、原作演出摘要、时序参数与复合弹型；玩家奥义、角色 Boss 和图鉴读取同一份演出定义，禁止按中文名或具体卡 ID 写运行分支。
- 可编号的原作符卡必须记录作品与具体 SC 编号；没有 SC 编号的自机 Bomb 必须记录作品、机体槽位并链接对应玩法页，不得只写“某作符卡”。

## Build And Progression

- 升级候选应自然受到本局已选亲和影响，但不能让单一路线百分之百垄断后续选择。
- 普通升级以 `当前重数/最大重数` 展示，最大重数通常为 2–4；满重后不再出现，单次提升必须明显，当前暂不设置互斥分支。
- 成熟构筑的三选一应同时保留精进、成势和补缺；内容包身份不进入强度或总类别权重，每轮最多出现一张奥义。
- 玩家弹幕数量与形态只能由升级、特化和奥义改变，压力阶段与存活时间不得自动赠送弹幕强化。
- 普通攻击分为预判普通弹与自机中心弹幕；两类共享单弹伤害、射击间隔和弹速，普通弹数量、弹幕数量及双方阵形分别成长。
- 当前 `追魂诀` 每重增加一发普通弹，`天罗弹阵` 每重增加四发中心弹幕；普通弹可选两翼收束，中心弹幕默认辐射，阵式特化分别形成二重、三重或同修后的四重螺旋。
- 任何阵形或临时效果都不能凭空增加弹丸数量；没有修习中心弹幕数量时，螺旋状态只保留其射速效果。
- 升级界面只负责选择与解释，不增加重掷货币、商店购买或额外主动操作。
- E 状态页的构筑图保持只读，可查看前置、互斥、亲和、奥义槽位和实时效果。

## Presentation And Engineering

- 逻辑视口为 `640 x 360`；HUD 与模态界面保持紧凑，避免整体滚动、可拖动分界线和内容挤压。
- UI 主基调固定为东方武侠像素工艺：深墨与漆木承载界面，旧金负责边缘和层级，朱砂只强调选中、危险与印章，青玉用于次级状态；禁止退回单线框、纯色矩形或现代圆角占位风格。
- 正式 UI 资产由 `tools/ui_asset_generator` 的确定性 RGB 像素绘制器维护，面板、按钮、页签、输入、列表、勾选、滚动、进度和地图框均使用可复现源代码生成；九宫格拉伸区必须保持平坦，角纹与木纹只能落在固定边缘区。
- 同类控件统一从 `WuxiaTheme.tres` 读取完整交互状态；普通、悬停、按下、禁用和键盘焦点必须可辨，页面不得自行复制一套近似但不一致的临时 StyleBox。
- 长名称先按真实字体宽度动态缩小，仍超宽时才显示明确省略号，详情区域保留全文；禁止按固定字符数无提示截断。图鉴核心属性和构筑详情必须在固定面板内直接可见，不依赖整体滚轮。
- 每次全局 UI 变更必须同时通过 640×360 边界断言与桌面 OpenGL 最近邻截图人工验收，至少覆盖主菜单、设置、内容选择、暂停、升级、图鉴、构筑、结算和地图。
- 所有像素纹理使用最近邻显示，角色、敌人、掉落物和后续结构不得模糊。
- 高实体战斗逻辑使用 ECS；低频菜单、状态页和遭遇编排可以继续使用 OOP Node。
- ECS 与 OOP 按实体数量、更新频率、生命周期和编辑器需求选择：高频批量状态进入 ECS，低频有身份编排留在 OOP；两侧只通过命令、事件和只读快照连接，不互持内部对象。
- 内容完成度分为 `inventory`、`development`、`complete`；当前 Base 与 TH06 为开发中，其余正作为迁移库存，只有通过完整内容包验收后才允许标记完成。
- C# 每个类单独文件，每个文件有效代码不超过 250 行，并为类与函数保留有意义的中文注释。
- F3 调试层显示当前压力档、综合难度指标、每秒刷新、四档敌群占比，以及三十秒滑动窗口的实时 `击破数/刷新数`；隐藏时不重建调试文字。
- 不主动导出或发布；只有用户明确要求稳定构建时才生成发行产物。
- 当前明确请求的正式验证产物为 `alpha-0.0.5`；导出时保留全部历史版本，不覆盖或删除旧构建。

## Documentation Authority

- `docs/plugin_first_design.md` 只负责产品架构、ECS/OOP 边界、内容包契约和迁移目标。
- `docs/combat_balance.md` 只负责玩家、Build、普通弹、中心弹幕、奥义和五分钟流程预算。
- `docs/enemy_balance.md` 只负责敌人种类固定属性、八档刷新、滑动窗口换档和 Boss 数值边界。
- `docs/diagnostics.md` 只负责诊断构建、复现、日志回传与判读；诊断构建不是独立游戏版本。
- `CHANGELOG.md` 保存历史发行事实，不能覆盖当前规则；README 只提供入口摘要，不重复维护整套数值。
- 内容完成度必须使用 `inventory`、`development`、`complete` 三态，禁止再把清单存在写成“已完成”。

## Settings Restoration — 2026-09-06

- 用户确认完整设置可以回归：对照旧版恢复音频、视频和当前游戏操作的双槽键位，不为尚未恢复的地图等功能放置无效选项。
- 保留现有成绩、音量与减少震屏偏好；画面修改提供确认/超时回退，重绑提供冲突反馈和默认恢复。
- 本次未明确请求导出新发行 EXE；先完成设置与验证，保留历史发行产物和更新记录。
