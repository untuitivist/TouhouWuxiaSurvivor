# Project Intent Notes

本文件记录已经确认的产品意图与工程边界；新实现应优先遵守这些定义，出现冲突时再与用户确认。

## Core Experience

- 游戏是东方 Project 同人、武侠风、像素表现的类幸存者游戏，使用 Godot C#。
- 项目采用“一切皆插件”的工程基调：内核、玩法规则和内容通过稳定模块契约组合，但玩家侧统一称为“内容包”，不称 DLC。
- 玩法规则由必选规则模块统一提供；内容包只能增加敌人、结构、角色、Build、奥义和表现，不能修改经验、压力、槽位、输入或胜负规则。
- 幻想乡本体是始终启用的基础内容包，必须独立形成完整游戏；东方红魔乡是第一份可关闭的参考内容包，用来验证完整内容契约。
- 当前优先级固定为先完成 Base-only，再完成 Base+TH06；其他作品保留资料但暂停新增玩法，不能用清单数量代替运行时完成度。
- 玩家局内只负责移动、走位和构筑选择；不增加需要主动按键施放的战斗技能。
- 奥义允许独立定时或战况被动触发，但不消耗灵力或其他充能资源。
- 本体必须形成完整闭环；每个作品内容包只横向增加地区、结构、敌人、角色与符卡选择，不提供纵向付费强度。
- 可选内容包彼此平行，不得依赖另一个可选内容包，也不得修改全局经验、压力换档、槽位容量或基础属性公式。
- 第一阶段内容包只携带声明式数据与素材，不执行任意 C# DLL；内部通用机制可以插件化，但玩家安装的是内容包而不是规则插件。
- 内容包通过稳定 ASCII ID、schema、内容版本和 host API 注册；中文名与译名只作显示，不能参与存档、引用、互斥或 Boss 排除。
- 若内容包需要新机制，必须先将其实现为所有内容都可使用、带数值预算和测试的通用能力，再由内容定义引用；通用系统禁止判断具体作品 ID。
- 已安装、已启用与本局激活是三个不同状态；开局后必须冻结规则版本、内容包版本与指纹、角色和世界种子，运行中不热加载或替换内容。
- 当前清单身份契约为 `schema_version = 1`、`host_api = content-v1`；Base 与 TH06 内容版本为 `0.1.0`，其余迁移库存为 `0.0.0`，只有显式能力声明可以启用对应通用机制。
- 图鉴读取已安装内容，本局世界、敌人、Boss 与升级池只读取冻结后的激活内容；禁用内容包后不得有任何运行时身份或素材泄漏。
- 局外存档中的缺包数据保留休眠；进行中的一局缺少原内容包时应明确拒绝续局并列出缺项，不能静默使用本体内容替代。
- 当前标准单局目标约五分钟；具体换档由玩家实际清场能力决定，击破最终角色 Boss 后成功结算，也可以保留构筑进入无尽游历。
- 每张已悟奥义都在底部 HUD 右上方显示独立文字印章；冷却遮罩从下边缘向上收缩，就绪后完全退去，被动条件尚未满足时显示待机边框。
- 自动射击必须读取目标运动速度并按实际弹速预判拦截；寿命内没有可靠交点时才回退直瞄。
- 有限流程的阶段推进只比较最近三十秒滑动窗口中的累计击破数 K 与实际刷新数 S；观察满三十秒且 `K >= ceil(0.90 * S)`、`S > 0` 才提高一档，失败后窗口继续逐帧滑动，不按互不相交的固定窗口结算。
- 不设置敌人存活软上限、换档超时或自动清场；总刷新率必须逐档严格上升，当前八档为 `2.40/3.15/4.05/5.10/6.15/7.20/8.25/9.30` 每秒。
- 当前刷新率是在上轮翻倍结果上再乘 `1.5`；只增加敌群供给，不改变各档强敌占比、怪物基础属性或 `K/S` 换档条件。
- 玩家选择无尽时记录实际进入时刻；之后每经过名义 30 秒，总刷新率在 9.30/s 基础上增加 0.12/s，最终配比不变且怪物种类属性仍固定。
- 敌群从 `100/0/0/0` 的普通敌人起步，最终保持 `40/30/22/8` 的普通/强敌/精锐/头目配比；难度主要通过更多敌人增长，强敌占比平缓混入。
- Boss 阶段继续使用最近目标和普通碰撞规则；不得为 Boss 增加专用穿透、保留弹或固定火力配额，接近 Boss 的通路由割草输出自然清开。
- 角色 Boss 基础生命统一乘 `6.0`，接触伤害与移动速度不额外提升；第一轮仅本体与红魔乡 Boss 使用自己所属符卡的图集、几何和阶段演出，其他作品保持通用弹幕。
- 本体保留六张横向奥义；红魔乡七名登记角色各有一张代表符卡，当前全目录共五十一张。后续作品按同一数据契约逐作补齐，不改变槽位容量。
- 怪物生命、移动、接触伤害、掉落和弹丸属性只由怪物种类决定，不随时间或压力档位增长；未来增幅怪必须作为明确的新种类或修饰类型设计。
- 普通弹始终围绕最近目标的预测交点发射；自机中心弹幕允许向外辐射或形成二至四重螺旋，负责近身割草而不参与锁敌。
- 每张奥义必须声明独立 `bullet_style` 美术语义；轨迹几何只管飞行与阵形，不能再用 `orbit/fan/line/ring/backstab` 猜弹丸外形。
- 原作 BulletAtlas 按 TH06、TH07-09、TH10-19 三套真实版式切片；刀、蝶、火焰、大玉等 32 像素帧必须整帧读取，禁止统一按 16 像素网格切碎。
- 普通自瞄弹使用针形，中心弹幕使用星形，敌弹在玉、碎晶、针、星之间轮换；它们共享切片目录但不共享错误的单一阴阳玉区域。
- 针、刀、光束、碎晶等方向型弹丸必须从实时速度推导姿态；追踪转向同步转身，阵式沿径向排布，对称弹不做无语义自转。

## Build And Progression

- 升级候选应自然受到本局已选亲和影响，但不能让单一路线百分之百垄断后续选择。
- 普通升级以 `当前重数/最大重数` 展示，最大重数通常为 2–4；满重后不再出现，单次提升必须明显，当前暂不设置互斥分支。
- 成熟构筑的三选一应同时保留精进、成势和补缺；内容包身份不进入强度或总类别权重，每轮最多出现一张奥义。
- 玩家弹幕数量与形态只能由升级、特化和奥义改变，压力阶段与存活时间不得自动赠送弹幕强化。
- 普通攻击分为预判普通弹与自机中心弹幕；两类共享单弹伤害、射击间隔和弹速，普通弹数量、弹幕数量及双方阵形分别成长。
- 当前 `追魂诀` 每重增加一发普通弹，`天罗弹阵` 每重增加四发中心弹幕；普通弹可选两翼收束，中心弹幕默认辐射，阵式特化分别形成二重、三重或同修后的四重螺旋。
- 任何阵形或临时效果都不能凭空增加弹丸数量；没有修习中心弹幕数量时，螺旋状态只保留其射速效果。
- 升级界面只负责选择与解释，不增加重掷货币、商店购买或额外主动操作。
- E 状态页的构筑图保持只读，可查看前置、互斥、亲和、奥义槽位和实时效果。

## Presentation And Engineering

- 逻辑视口为 `640 x 360`；HUD 与模态界面保持紧凑，避免整体滚动、可拖动分界线和内容挤压。
- 所有像素纹理使用最近邻显示，角色、敌人、掉落物和后续结构不得模糊。
- 高实体战斗逻辑使用 ECS；低频菜单、状态页和遭遇编排可以继续使用 OOP Node。
- ECS 与 OOP 按实体数量、更新频率、生命周期和编辑器需求选择：高频批量状态进入 ECS，低频有身份编排留在 OOP；两侧只通过命令、事件和只读快照连接，不互持内部对象。
- 内容完成度分为 `inventory`、`development`、`complete`；当前 Base 与 TH06 为开发中，其余正作为迁移库存，只有通过完整内容包验收后才允许标记完成。
- C# 每个类单独文件，每个文件有效代码不超过 250 行，并为类与函数保留有意义的中文注释。
- F3 调试层显示当前压力档、综合难度指标、每秒刷新、四档敌群占比，以及三十秒滑动窗口的实时 `击破数/刷新数`；隐藏时不重建调试文字。
- 不主动导出或发布；只有用户明确要求稳定构建时才生成发行产物。
- 当前明确请求的正式验证产物为 `alpha-0.0.5`；导出时保留全部历史版本，不覆盖或删除旧构建。

## Documentation Authority

- `docs/plugin_first_design.md` 只负责产品架构、ECS/OOP 边界、内容包契约和迁移目标。
- `docs/combat_balance.md` 只负责玩家、Build、普通弹、中心弹幕、奥义和五分钟流程预算。
- `docs/enemy_balance.md` 只负责敌人种类固定属性、八档刷新、滑动窗口换档和 Boss 数值边界。
- `docs/diagnostics.md` 只负责诊断构建、复现、日志回传与判读；诊断构建不是独立游戏版本。
- `CHANGELOG.md` 保存历史发行事实，不能覆盖当前规则；README 只提供入口摘要，不重复维护整套数值。
- 内容完成度必须使用 `inventory`、`development`、`complete` 三态，禁止再把清单存在写成“已完成”。
