# 版本日志

版本号采用 `{stage}-{major}.{release}.{optimization}`：`stage` 是开发阶段，`major` 是大版本，
`release` 是发布序号，`optimization` 是同一发布线的优化序号。阶段只允许 `alpha`、`beta`、
`rc` 或 `stable`。运行时版本唯一来源是
`project.godot` 中的 `application/config/version`，集成测试会校验本文件的最新版本标题。
导出文件名必须完整包含该版本；Windows 四段数字版本固定映射为
`major.release.optimization.0`。

## alpha-0.1.8

交付维护（2026-09-11，不升游戏版本）：Web 导出排除已经静态链接进引擎的 26 个编译库，修正资源包与下载清单；核心 gzip 下载量由 32.54 MB 降至 21.61 MB（减少 33.6%）。保留全部玩法 DLL、画面、音乐、字体、语言数据和原始导出，289 个运行内容逐字节不变；后续构建自动执行同一检查。此项仅减小首载包体，不代表远程直连线路已提速，Windows EXE 无需重下。

同日传输维护：直连有大量重传和发送窗口过小问题，使用服务器现有内核的 BBR，作用于新建 TCP 连接，不重启 Caddy 或更改其他服务路由。同资源无代理 A/B/A 对照从约 17–18 KB/s 提高到约 290–296 KB/s；完整无缓存浏览器实测 20.55 秒进入标题，21.61 MB 正文完整接收。仅代表本机当次直连环境，不保证所有网络同速；配置与回滚方式见部署文档。

发布内容：魔理沙重做为少量高频的持续星流、药菇续航与魔炮联动。Windows 自带依赖单 EXE 与无需共享内存的 Web 继续使用同一份 C# 玩法，Windows 文件版本为 0.1.8.0。

- 星流：开局仅星流，基础每 0.18 秒向四方逐颗散星、存活 8 秒，每 0.12 秒近身撕扯；命中不消耗星体，不再一次发一团。慢移不改变四方出星方式。
- 随机质量：每星独立从质量分布抽样，不设总质量池、不保底大星、不指定行星名额。增质提高分布中位数、变谱扩大离散程度、长明延长新星寿命，三项可反复修习并显示重数；已有星体不重抽、不续命。
- 多体引力：星与星、星与附近全部存活怪物按质量双向作用；毛玉/妖精/冲锋/精英/Boss 质量分别为 12/20/40/80/260。重星可牵引 Boss，没有首领专属免疫；不增加怪物与怪物全量引力。96 星为运行容量而非总质量预算，满载不积攒齐射。
- 数值：低单次不代表低总输出。单位质量撕扯强度独立精进，星体质量参与真实伤害；同投入 0/4/8/12 点的三种子持续单体 DPS 与灵梦相差约 0%–4%。另测近远、移动和群体；早期群伤与近身多发命中仍各有优势，不宣称所有场景完全相等。口径和数据见 docs/marisa_growth.md。
- 药菇：修习解锁后拾取回血，满血保留；留药与缓释可兼修。在场数量、寿命、每秒恢复和待恢复药量均有上限，不做无限吸血。
- 魔炮：修习解锁 Master Spark，基础锁向，可兼修追敌、广域、星光共鸣和限量消弹。追敌自动瞄准最近存活敌人，停步也会追踪，不跟随移动方向；共鸣只在实际火线中增强星体撕扯，不增质量、不续命。
- 角色与素材：魔理沙基础生命为 100，移除旧 16% 通用伤害加成；两种语言、选人、修习、构筑、图鉴同步更新。优先原作素材，药菇使用《刚欲异闻》魔理沙蘑菇，经 Aseprite 原色转 PNG，保留来源校验；自绘尝试只归档。灵梦、原有键盘/触屏操作与视角保留。
- 色彩表现：原作星星接入 14 种颜色，每星颜色固定，外观随机不干扰质量和伤害；魔炮保留原作纹理，改为多色同时出现、沿炮身流动的炫彩。暂停冻结流动，减少动态效果保留静态彩虹；不改攻击判定或 DPS。
- 架构与验证：密集组件、空间网格、20Hz 多体力场与 60Hz 积分，共用批量绘制，无逐星场景节点。回归覆盖多体互易、随机分布、长期成长、Boss 牵引、独立生命周期、回血限额及最近敌人魔炮；压力测试含 320 敌人/1600 弹幕/96 星。原生核心耗时不能冒充 Web/手机 FPS。
- Web 发布修复：兼容模板此前在正式导出时仍启用 Mono 调试，关闭了解释器优化；现仅调试构建启用调试，正式 Web 使用优化运行时，继续支持无跨源隔离、无共享内存。高密度星体伤害只合并飘字，不合并真实逐星伤害；密集组件遍历减少结构复制。
- Web 热点修复：多体引力和阻尼积分改为等价标量运算，避免解释执行中的大量向量运算开销；不降低星体容量、怪物数量或相互作用范围，不改变质量分布与 DPS。
- 历史：保留全部已发布日志、旧 EXE 和失败候选证据。旧碰撞样板归档于 docs/marisa_collision_sample_20260910.md；不启用未通过验证的 WebGPU。

## alpha-0.1.7

发布内容：Windows 自带依赖单 EXE 与无需共享内存的 Web 使用同一 C# 源码；Windows 文件版本为 0.1.7.0。本版基于正式 alpha-0.1.6，加入两位角色的临时选人立绘，保留旧版操作、玩法和完整更新记录。

- 角色选择：暂用灵梦、魔理沙两张 AI 图片中的完整人物预览，独立图片区域保持人物比例；保留角色文案、出发与返回按钮，支持中英文、键盘和触控。
- 素材范围：仅选人界面使用 AI 图片；局内人物、弹幕、技能、场景、标题、图鉴、数值与视角不变。一般素材方向仍为 Aseprite＋原作素材，不重新启用已撤回的美术试验。
- 可编辑源文件：原图完整保存在 `ai-assets/`，裁切与透明背景处理源文件在 `art/portraits/ai-preview/`，保留参考层和可编辑前景层；这是 AI 图片裁切，不是人工重绘。Windows/Web 使用相同导出图片。
- 发布验证：选人专项检查两位角色回调、返回、布局和触控；独立 EXE 增加选人截图，保留中英文、无共享内存、下载计量、战斗与贴图颜色回归门槛。
- 历史保留：发布前按要求回退到 alpha-0.1.6 正式内容，旧版 EXE、原图和试验源稿均保留；已撤回美术的历史记录见 `docs/withdrawn_visual_changelog_20260909.md`，不列作本版新增功能。

## alpha-0.1.6

发布内容：Windows 自带依赖单 EXE 与无需共享内存的 Web 使用同一 C# 源码；Windows 文件版本为 0.1.6.0。此次纳入中英文切换、文案整理和 Web 实体密集战斗优化，旧版产物与历史记录保留。

- Web 战斗热路径：距离、方向、位移、击退与扫掠碰撞采用共用标量计算；精确碰撞前先做保守包围盒排除，不降低敌人／弹丸上限、伤害、技能预算或物理频率。
- 复用既有 ECS/OOP 边界：战斗空间网格在有限场地内使用直接索引，场外保留兼容桶；只清理已占用桶，保持候选顺序、死亡过滤和零分配查询。
- 寻敌先比较距离，再检查命中排除历史；消弹增加保守包围盒粗筛；实体回收跳过未变化的前缀，保持存活实体顺序并减少无效复制。
- 追踪转向直接计算最短角度差，御札贴图直接使用速度方向构造旋转；减少重复三角计算，新增数学等价和旋转贴图对照验证。
- 批量绘制：固定尺寸敌弹与拾取物只更新位置，静态矩阵／颜色在扩容时初始化；缓存角色与弹型批次，避免逐实体重复字符串查找。保留显式顶点颜色及变色／扩容／清空重用验收，防止贴图发黑回归。
- 中英文切换：设置页可切换简体中文／English，立即生效并保存。覆盖菜单、完整设置、HUD、升级、构筑、图鉴与诊断；旧存档默认中文，不清除成绩，不恢复暂停中的战斗。刷新 Web 后保留语言。
- 文案与场景：精简按钮、提示和结算文案；移除阵法上重复的名称、封／定与净化提示字，保留阵纹、进度环和任务栏。历史更新记录保留中文原文，下载门户仍独立维护。
- 发布门槛：新增中英文、无跨源隔离的三档动态实体压力，分开检查真实帧率、模拟速度、逻辑和批量提交耗时；静态截图不再代替游玩性能验收。Windows 单文件验证加入语言专项，Web 检查真实设置操作与双向刷新持久化。
- 验证边界：桌面浏览器触控模拟不是手机真机承诺；未改变人物设定、成长取舍、原操作或原作／Aseprite 素材来源。后续仍需不同手机实测。

## alpha-0.1.5

发布内容：同一干净 C# 源码交付 Windows 自带依赖单 EXE 与无需共享内存的 Web 更新，Windows 文件版本为 0.1.5.0。本版纳入灵梦成长树、首轮平衡与 Aseprite 像素美术；保留旧版、操作与完整历史日志。尚未提交的中英文切换不包含在此版本。

- 界面美化：Aseprite 重绘 13 份 UI 素材，新增主按钮悬停/按下、朱砂书签、缝线分隔与淡色花枝共 5 份。素纸底纹平铺，深青漆边与旧金细线替代厚重褐框；主按钮浅字深底，保持原有布局、触控和数值。图鉴检查区分装饰与内容预览，仍验证数量与裁剪。

### 美术来源统一：原作 + Aseprite

- 保留原作角色、敌人、弹幕、技能、草地、树木与鸟居；不再运行时生成 UI 纹理，整套菜单/按钮/焦点/滑块皮肤改用 Aseprite 分层素材。
- Aseprite 重绘石板路、古印石座、飘落花瓣、角色阴影和触屏控件；图标保留原作阴阳玉、符札与星星，在 Aseprite 内重绘边框并合成。新增 21 份可编辑源文件，旧素材不删除。
- 双端共用素材；导出过滤排除已停用旧图标与程序地砖，Web 暂存加入新素材。文字、进度、预警与判定反馈仍按实时数值绘制，操作和战斗平衡不变。

- 标题背景：按旧版彩色像素夜景方向，改用本机 Aseprite 内置脚本分层绘制月亮、鸟居、樱树、暖灯与石阶；保留可编辑源文件，运行时只加载导出贴图。Windows/Web 共用同一图片，不再给标题背景叠加蓝灰染色。原黑白素材、局内场景、角色、菜单和操作均保留。

- 灵梦成长样板：开局只带直射御札；封魔阵、阴阳玉和梦想封印需在修习中解锁。魔理沙保持本轮之前的行为，尚未迁移成长树。
- 可兼修行为：御札追踪与命中爆破；封魔阵敌群选点与禁锢；阴阳玉有限消弹与自动蓄力发射。普通妖怪受禁锢停止位移，首领只减速且继续弹幕；发射期间少一枚护身玉，不增加主动按键。
- 首轮数值平衡：修复偶数扇形御札漏过远处目标，保持弹数但保留一发中央瞄准弹；追踪减少单札伤害 20%，爆炸只向最多 4 名其他敌人溅射 35%，不重复伤害直击目标。新增同投入场景对比和三路线六种子评估，保留阵控场、玉近身与消弹的优势，不将自动通关等同于玩家胜率。
- 构筑解耦：升级使用稳定节点 ID、前置、次数上限与共享定义，能力重数与行为分别记录。三选一过滤未解锁分支，优先保留能力解锁和行为选择；暂停、构筑和原有图鉴详情同步显示成长信息，保留旧日志。
- 成长树技能沿用已接入的原作御札、阵纹、阴阳玉与光环；新增和修改的原创美术仅使用 Aseprite。
- 验证边界：自动回归不等于玩法乐趣或手机真机验收；发布验收与产物来源另记于部署文档。

## alpha-0.1.4

发布内容：同一 C# 项目的 Windows 自带依赖 EXE 与单线程 Web 同源更新；Windows 文件版本为 0.1.4.0。发布卡片图鉴、触控小地图避让及原作场景素材，保留全部旧版与历史日志。

- 图鉴回归：标题与暂停菜单新增“夜境图鉴”，六类共 22 项当前内容改为素材卡片，点击打开独立详情；返回保留分类、页码和焦点。详情含逐重术式、角色与敌人基准、设定改编和素材路径，支持滚动与触屏上下阅读。只投影当前玩法，旧版未迁回的地图和作品包不冒充可玩内容；浏览不推进战斗。

- 小地图遮挡：触控按钮显示时，小地图与地名移到右上暂停按钮下方；隐藏触控后恢复原桌面位置。保持暂停、构筑按钮及触摸范围不变，支持自动检测和显示模式切换；Windows/Web 共用布局，保留原有标记和地图比例。
- 场景素材：树木改用心绮楼原作树冠贴图，鸟居改用原作透明素材，标题背景改用凭依华博丽神社插画，移除原先的程序树木、鸟居、山景和飘落方块。保留菜单、角色精灵、原树木分布与全部战斗规则；鸟居作为无碰撞装饰略向下调整，避免开局被 HUD 遮住。
- 素材与可读性：四张图片逐字节保留原作像素和透明度，记录来源与哈希；运行时缓存加载、按比例绘制，树冠和鸟居继续位于敌弹及角色之下。标题保留文字衬底；原插画为黑白神社画面，不声称复刻夜间场景。图鉴大图按比例收进预览框，不遮住卡片文字。
- 验收边界：保持无共享内存 Web、完整设置、F3 与下载计量及 VPN 条件性提示。浏览器触屏模拟不代表手机真机验收，不新增帧率或全设备兼容承诺；不改变战斗数值和角色能力身份。

## alpha-0.1.3

发布内容：同一 C# 项目的 Windows 自带依赖 EXE 与 Web 原作战斗美术更新；Windows 文件版本为 0.1.3.0，保留全部旧版和更新历史。

- 网页入口补充（2026-09-08）：下载卡片提示网络较慢时可尝试开启 VPN 连接外网或切换线路，可能改善下载速度；不保证加速。仅更新网页加载文案，不改变本版 EXE、游戏资源包或玩法。
- 原作美术接入：魔炮本体和蓄力星环改用永夜抄魔理沙图集；封魔阵纹、四角符札采用妖妖梦灵梦图集，符卡光环采用永夜抄灵梦图集，古印使用天空璋通用阵纹。已有原作星弹保留，剩余程序五角星光效改用贴图。
- 合成与范围：魔炮分层发光，阵纹分离白底并保留原始裁图；记录全部原图、裁切和处理方式。保留前摇范围提示、净化进度与判定点等功能标记，不改变伤害、弹数或操作。标题、树木等其他程序美术尚未替换，阵纹仍偏密；不将素材接入等同于整体美术已完成。
- 验收边界：发布产物补验角色特效与已有颜色回归，继续保留无共享内存 Web；手机真机性能与此前发黑的真实设备触发条件仍待复测，不作新增兼容性或帧率承诺。

## alpha-0.1.2

发布内容：Windows 与 Web 同源更新批次颜色修复；Windows 文件版本为 0.1.2.0，保留 alpha-0.1.1 及全部历史日志和成品。

- 实体发黑排查：批量贴图改用带明确白色顶点颜色的共用网格，避免依赖默认颜色状态；保留实例染色、透明度、动画、旋转与先前的边界修复，不减少实体、不修改玩法。Windows 与 Web 使用同一实现。
- 回归补充：加入黑色和彩色图形交错绘制、默认顶点色异常注入检查，覆盖两位角色完整流程。注入条件下旧实现出现黑块或消失，新实现通过；用户截图对应的真实设备触发条件尚未独立复现，不宣称所有发黑已彻底解决。正式 EXE 检查交错绘制，Web 同时检查普通与异常注入条件。

## alpha-0.1.1

发布内容：Windows 与 Web 同源发布实体贴图消失修复，保留 alpha-0.1.0 及全部旧版记录和成品。

- 实体绘制：动态批次在提交时同步当前可见实例的紧致边界，包含旋转后的贴图范围，避免战斗中实体仍在但整批贴图被错误裁掉；保留批量绘制，不减少敌人或弹幕。修正四边形与普通精灵的纵向贴图坐标差异。
- 回归检查：增加两位角色长局的批次与普通逐实体绘制对照，检查扩容、清空后复用、换帧、受击染色与资源回收，并对实际导出的 EXE 和 Web 产物执行检查。用户报告的发黑尚未单独复现，不保证所有发黑都由同一原因引起；仍需实际复测。

## alpha-0.1.0

版本决定：2026-09-07，用户确认双端正式发布使用 alpha-0.1.0，发布序号提升、优化序号归零；保留 alpha 阶段。alpha-0.0.10 仅为本地未发布候选，不计为线上历史版本。

发布内容：同一项目双端交付，自带依赖 Windows EXE 与对应网页更新部署。本版合并批量战斗系统、绘制优化、原作素材与新图标、完整下载进度，以及真正的单线程 C# Web 兼容构建。

- 单一项目：Windows 与 Web 共用游戏源码、场景、素材、设置和触屏操作；平台差异只在构建与系统接口。保留完整设置、像素界面、F3 和人物独立能力，不新增第二套玩法。
- 网页兼容：发布单线程引擎与匹配的 C# 原生调用签名，不再要求跨源隔离或 SharedArrayBuffer。仍需支持其余引擎能力；小米浏览器真机与手机帧率尚未验收，不宣称所有移动浏览器均已通过。
- 战斗更新采用轻量组件存储与批量系统，菜单、输入与低频编排保持 OOP；不减少敌人、不暗改数值来换取帧率。地面与战斗对象批量绘制，Web 控制实际渲染尺寸。桌面对照数据不代表手机真机帧率。
- 首次加载显示核心资源总量、已接收量、百分比、实时接收速度和预计剩余时间；完整接收后再进入初始化，连接停顿不制造假进度。下载流保留原始响应并读取到结束，不忽略资源中止事件。
- 发布门禁同时检查有隔离与无隔离、原始资源与部署 gzip 路径，覆盖键鼠、多点触控、保存刷新、音频、升级和两位角色完整流程。更新日志测试按所选版本检查，旧版设置与 F3 记录仍保留。
- 旧版 EXE、旧图标与全部更新记录保留。浏览器与 Windows 存档不自动同步；Android/iOS 原生安装包不在本次范围。原作素材仅说明来源，不代表取得再分发授权；本作仍是开发阶段的非官方原型。

### 开发过程记录

以下保留此前未发布记录原文；其中“未部署”“不导出”等描述仅代表对应开发阶段，不是本次交付状态。

- 同一项目兼容验证：Windows 与 Web 继续共用一套 C# 游戏、场景与操作；新增不需要跨源隔离和 SharedArrayBuffer 的单线程 Web 构建。修正实验引擎的原生调用签名生成，未通过改玩法绕过问题。
- 网页兼容测试：有隔离和无隔离环境下，键鼠、三指触控、设置保存、音频启用、旋转暂停、升级及灵梦和魔理沙完整流程通过；下载进度与错误提示回归通过。小米浏览器真机和原生移动端尚未验证，不承诺手机帧率；本次未部署、不改版本号、不导出新 EXE。

- 移动端性能：实际战斗代码接入密集组件存储和敌人、弹幕、掉落物系统，菜单与关卡编排继续使用 OOP；空间查询不再创建迭代对象，追踪弹按身份查找目标，减少逐帧分配。保留攻击、碰撞、刷新上限与原有操作，不用减少敌人换取帧率。
- 绘制优化：地面、敌人、弹幕与掉落物按组提交，静态场景保留绘制结果，暂停时不重复重建战斗画面；Web 固定逻辑渲染尺寸，避免高像素密度设备放大绘制负担。F3 增加批量绘制数量、提交耗时及实际渲染尺寸。性能数据来自本机对照测试，不作为手机或平板的帧率承诺。
- 原作素材：优先采用用户素材包中的 TH10 草地、符札、星光、弹丸与道具贴图，保留来源、裁切范围和校验记录。更换游戏与导出图标为原作阴阳玉、符札及星光组合的像素徽记，保留旧图标文件；公开发行授权仍需单独确认。本次仅日常开发，尚未推送或部署，不提升版本号、不导出新 EXE。

- 网页加载：首次进入显示核心资源总量、已接收量、百分比、实时接收速度与预计剩余下载时间。部署时按实际压缩文件大小生成清单；下载完成后单独提示解压与初始化，连接停顿不增加进度，失败可重新加载。手机竖屏加载时也能查看进度。此项尚未更新线上，不改版本号、不导出新 EXE。

- 后续部署：已在 `https://allinagent.top/TouhouSurvivor/` 完成首次网页试部署，桌面开局、触控、刷新保存通过；新增 GitHub clone/pull 更新工具与回滚检查。正式发布同时交付 Windows EXE 和网页更新部署。本次不改版本号、不重新导出 EXE；下方未部署描述保留为前期记录。

- 同一项目双目标：保留一套 C# 玩法、场景、素材与存档结构，新增 Web 构建入口；自动生成的隔离目录只解析实验 Web SDK 与目标框架，不手工维护第二份游戏。Windows 保持原工具链与自带依赖 EXE 发布流程，本次不改版本号、不导出新 Windows 发行版。
- 平台基础：两端统一使用随包中文字体，附带字体许可证及来源校验；存档改为源生成 JSON 与 Godot 文件接口，保留旧记录兼容、坏档保护和即时保存，避免浏览器裁剪反射导致读写失败。
- 手机操作：增加独立多点触控摇杆、慢移、闪身、暂停与构筑入口；主菜单、选人、升级和设置扩大关键触摸区域。新增触控设置页与手机 F3 入口，原键盘操作和改键保持独立；竖屏提示横屏并安全暂停。
- 平台设置：Web 隐藏无效桌面窗口设置，提供浏览器全屏、帧率和减少震屏；声音在首次用户输入后启用。Windows 保留完整显示设置与预览回退。
- 限制：Web 使用隔离的 4.6.1 社区实验工具链，需要 HTTPS 与跨源隔离响应头；浏览器记录与 Windows 记录不自动同步。触控模拟不等于 Android/iPhone 真机验收；未上传服务器，素材公开发行授权问题仍需单独处理。

前期验证记录（保留原文）：

- 新增隔离 C# Web 验证工具与报告：实际对比 4.7.1 / 4.6.1 实验工具链、域名子路径托管、桌面浏览器与触控模拟，记录渲染、中文字体、JSON 存档和手机操作限制。当前不满足公开部署条件；未改变正式游戏代码、版本号或 Windows EXE。

## alpha-0.0.9

发布内容：合并完整设置、像素界面与 F3 诊断。Windows 单文件内嵌游戏资源与 .NET 运行依赖；保留历史日志和 EXE，不推送远端。

### 像素界面与 F3 诊断

- UI 改为原创暖纸、木框、朱红书签与鼠尾草绿选中态；面板、按钮、下拉框、滑块、滚动条和提示框共享像素皮肤，覆盖普通、悬停、按下、禁用与键盘焦点。像素框和图标按最近邻绘制；标题采用硬边字形，中文正文保留灰度抗锯齿以兼顾小窗口可读性，并未引入专用位图字体。
- 主菜单重绘为 320×180 原生像素夜景：月色、鸟居、樱树、石阶与灯笼；以最近邻放大，保留原有角色素材。减少震屏同时停止菜单飘落像素。战斗 HUD、进度条与小地图边框统一风格，但不改地图规则、攻击行为与敌弹危险色。
- 参考方向为用户指定的《东方夜雀食堂》，但参考页/图片抓取不稳定，当前是原创暖色像素探索稿，不声称逐图还原或复用该作素材。
- 恢复默认 F3 的只读诊断层，采用类似 Minecraft 的左右文字栏与帧间隔图。显示实际 FPS、引擎主循环/物理耗时、2D 坐标、世界种子、模拟 Tick、敌人/敌我弹/掉落计数、角色与战况、窗口/渲染/GPU 信息及两种明确区分的内存统计。不虚构三维区块、旧压力档或 GPU 耗时，不提供作弊操作。
- F3 不暂停游戏、不改变升级或构筑状态，菜单中也可查看；文字每秒最多刷新四次，帧图按真实可见帧采样，关闭后停止采样和文本重建，重新开启清空旧帧图。诊断截图是受控场景，不用于性能结论。
- 操作设置扩展为十三项/二十六个槽位，F3 可重绑，帮助与覆盖层提示同步。新动作不会抢走旧存档已用于其他操作的 F3；捕获键位时先处理冲突，不穿透开启调试层。
- 本版不改角色能力、地图流程或战斗数值，不新增地图、图鉴、无尽模式或 Web 版；操作数量包含新 F3，共十三项。

### 完整设置回归

- 完整设置在日常提交中完成，本次按用户发布指令合入 alpha-0.0.9；不覆盖 alpha-0.0.8 或更早成品。
- 设置按声音、画面、操作分为三页，标题与暂停入口统一为“游戏设置”。保留三路音量、独立音乐/音效静音和减少震屏；静音不清空滑块值。
- 恢复窗口化、无边框窗口、全屏，640×360 至 3840×2160 七档窗口分辨率，垂直同步，以及 30/60/120/144 FPS 和不限制。窗口尺寸大于当前屏幕可用区域时收缩；无边框改为保留任务栏的最大化窗口，全屏单独负责铺满显示器。
- 画面选项先编辑、再应用，15 秒内确认才保存；超时、Esc 或撤销会恢复此前已确认设置。离开页面丢弃未应用选项；全屏快捷键也走预览确认，战斗中自动暂停并按原状态返回。
- 恢复双槽键位设置，覆盖十三项操作：四向移动、慢移、闪身、暂停、构筑、全屏、F3 诊断和三项升级选择。改键立即生效，支持冲突提示、Esc 取消、Delete 清空副槽；每项至少保留一键。Tab/Delete 保留给界面，Esc 始终保留安全返回，不支持组合键或鼠标/手柄重绑。
- HUD、行走须知、构筑返回与升级按钮显示实际绑定，升级选择优先于焦点按钮处理，避免改到 Enter 后错误选择第一项。
- 每页独立恢复默认并二次确认，不清除成绩或其他页设置。重写版已有 profile.json 继续使用 schema 1，新增字段缺失时补默认，非法尺寸/帧率/键位会规范化；损坏原文件仍保留并显示警告。
- 范围说明：没有恢复完整地图、图鉴或调试作弊功能，因此不放置对应空键位；重写前 user://settings.json 保留原位，本轮不自动导入其中旧操作配置，避免覆盖当前偏好。历史发行日志和 EXE 原样保留。

## alpha-0.0.8

### 人物设定纠偏：不是给旧攻击改名字

- 武侠保留为气质、进退与取舍，不再用统一御剑/雷法覆盖东方人物身份。依据 docs/character_identity.md 收录的 THBWiki 原作资料核对，区分符卡出处与本作幸存者改编。
- 灵梦初始为追踪御札与阴阳玉，可修习驻留封魔阵。御札持续转向并在目标消失后重新索敌；阴阳玉绕身接触攻击；封魔阵留在施放位置，对方形区域内的敌人周期生效，不随玩家移动。
- 魔理沙初始为星光射击与 Master Spark，可修习星尘幻想。星光射击为直线扇形星弹，Shift 慢移收束角度，不额外增加弹数或伤害；星尘幻想向四周散射并可贯穿两敌，不再使用跳跃雷链。
- Master Spark 是真正的持续方向光束：0.45 秒蓄势提示，随后按 0.12 秒伤害脉冲照射有限长度与宽度的前向区域，并清除光束内敌弹。施放期间方向锁定，玩家仍可移动以平移火线；施法精进只缩短魔炮休整，不加快脉冲或前摇。
- 统一“剑意爆发”替换为符卡蓄势与各自的代表性效果。灵梦满蓄势发射七枚追踪灵光并在命中处爆破，对应梦想封印的本作转译；魔理沙满蓄势施放更宽、持续 2.4 秒的强化 Master Spark。发动时的清弹、短暂无敌和经验吸取保留为共同辅助规则，不声称两张原作符卡完全相同。
- 每名角色现在有 3 条专属能力与 4 类通用修习；两人合计 6 条专属能力。相比上一版每人共用 4 门攻击，本版个人可选攻击数减少一条，但不再共用整套攻击池。候选生成与选择应用两层都检查角色归属。
- 新增共享 AbilityTuning，攻击与升级数值说明读取同一份配置；构筑页和 HUD 按角色列出能力，不再硬取枚举前四项。三选一和 E 构筑页展示原作出处及本作改编说明。
- 御札为符纸轮廓，魔理沙使用旋转星形弹、魔炮瞄准提示及光束，梦想封印为七色灵光，封魔阵有独立驻留边界。敌弹保持在玩家攻击表现上方绘制，避免魔炮完全遮住危险。

### 保留、调整与边界

- 保留 WASD/方向键、Esc/P、E 构筑与原状态返回、1/2/3 选择、音量设置、历史日志与旧记录。没有新增强制主动施法按键；存档只记录偏好/成绩，不保存正在进行的构筑。
- 显示标题改为“幻想乡 · 夜境异闻”，清理强加的剑客文案。项目内部 application/config/name 仍保留旧字符串作为存档目录命名空间，窗口标题另行设置，避免旧偏好与成绩失联。历史成绩继承不表示新旧规则可直接比较。
- 终局敌人明确为本作原创“结界残影”，不再把通用弹幕敌人直接命名、绘制成魔理沙。地图、三处古印、240 秒终局到来、胜败与重开闭环保留。
- 本版调整的是两名当前可玩角色，不是全部东方角色的全量制作；符卡的自动施放、数值与几何是本作改编，不是原作逐帧复刻。无限地图、全作品内容、局外永久数值成长、键位重绑与 Web 移植仍未恢复。
- 26 项核心回归覆盖角色初始能力与升级池、御札追踪/重新索敌、星弹收束、驻留阵地、光束前摇/范围/伤害间隔/清弹，以及暂停/三选一期间的持续能力冻结；保留 UI 与旧记录兼容测试。
- 两名角色各三种种子的自动导航流程均通关。自动测试与演示截图只证明对应流程和表现可运行，不替代真人操作手感、难度或乐趣验收。
- 新交付沿用 TouhouWuxiaSurvivor_alpha-0.0.8.exe，Windows 版本 0.0.8.0；完整历史日志随包读取，旧版条目与 EXE 不覆盖。

## alpha-0.0.7

### 操作与信息体验：按价值重建，而非逐项复刻

- 保留 WASD / 方向键移动、Shift 慢移、Space 闪身；恢复 Esc / P 双暂停入口、E 属性与构筑入口，输入集中到动作映射。自定义重绑尚未恢复。
- E 可从战斗、暂停或升级三选一打开构筑页。查看期间时间停止；关闭后回到原来的战斗、暂停或同一组升级候选，不消耗选择、不刷新候选。查看构筑时数字键不会误选背后的升级。
- 构筑页显示本局实际生命、武学威力、施放频率、移动速度、拾取半径、闪身冷却、四门武学与四类心法/身法重数，并说明下一重的变化或圆满形态。不将施放频率虚称为 DPS。
- 三选一改为描述这一次升级的具体变化，包括飞剑数量与穿透、阴阳玉数量与范围、灵符数量、雷链目标与最终重间隔；增加不打断选择的构筑查看按钮。
- 统一子页面返回：设置、更新记录先回到暂停；取消「结束本局」不直接恢复战斗。导航快捷键在按钮获得焦点时仍生效，打开模态页会清除待执行闪身。
- 音画设置加入总音量、音乐音量、音效音量滑块，支持键盘微调、即时生效和本机保存；保留音乐/音效静音及减少震屏。已有 alpha-0.0.6 记录与静音偏好兼容，缺失音量字段使用原有响度。
- 标题和暂停菜单恢复游戏内更新记录，直接读取随 EXE 打包的完整 CHANGELOG.md，默认展示当前版本，可按版本切换、浏览完整历史、滚动与复制。alpha-0.0.0 至 alpha-0.0.6 的历史条目原文保留，不另维护一份缩略历史。

### 本轮没有做的内容

- 没有改变战斗数值、敌人节奏、地图规模、角色数量或胜负条件；闪身、擦弹、古印继续保留供后续体验评估，不因已实现就认定必须长期保留。
- 没有机械恢复无限地图、M 大地图、F/Home 地图定位、F3 调试页、六类图鉴、全部作品库存、永久数值成长、无尽模式、双键位重绑或 Web 移植。这些仍需各自证明玩家价值。
- 当前构筑页是信息查看，不是装备切换或新的局外成长系统；保留旧操作习惯不代表复刻旧架构。
- 本版沿用自带依赖 Windows 单 EXE 交付口径，版本为 alpha-0.0.7 / 0.0.7.0，不覆盖任何旧版 EXE。自动化验证不等于真人可玩性验收。

### 验证范围

- 20 项核心回归；真实引擎输入分发检查暂停/返回、构筑查看与候选保留，音量控件及旧记录兼容、损坏记录保留、完整日志读取检查。
- 12 类实际渲染画面，以及 960×540 下的三选一、构筑、满重构筑、设置、更新记录。无头音量测试只验证控件、音频总线与持久化，不等于真人试听。

## alpha-0.0.6

### 版本定位

- 延续既有 alpha 开发序列，大版本与发布序号仍为 0.0，优化序号推进到 6；不因重写自行更改阶段命名。
- 本版本从零实现「幻想乡 · 剑雨异闻」内部试玩闭环，旧源码与旧成品保留，但不参与新运行时。

### 玩法与表现

- 灵梦与魔理沙两名行者，自动御剑、擦弹蓄势、闪身与四门可成长武学。
- 单张博丽夜境、三处净化古印、精英遭遇、三阶段终局 Boss，以及胜败结算和重开。
- 重建菜单、HUD、升级三选一、音画设置与本地成绩记录。

### 内部交付

- Windows 成品命名为 `TouhouWuxiaSurvivor_alpha-0.0.6.exe`，文件版本 `0.0.6.0`；沿用 PCK 与 .NET 构建输出内嵌，不以源码启动脚本替代交付。
- 旧版功能不是逐项迁移；素材仅用于内部验证，本次导出不代表公开再分发授权。

### 新增机制与实质变化

- 新增 Space 闪身与冷却、Shift 慢移与判定显示；旧版无须主动战斗按键，新版增加了主动生存操作。
- 新增每颗敌弹只计一次的擦弹、剑意蓄积，以及满槽自动范围攻击、清弹和吸取经验。
- 新增天、地、人三处古印，停留净化并保留进度，完成后回血、悟道并吸取经验。
- 构筑改为御剑、阴阳阵、灵符、雷法四门五重武学，以及四项通用修炼和恢复选项；替代旧版的六路有限/无尽修行、阵形特化与独立奥义槽位。
- 节奏从最近三十秒 K/S 清场达标推进，改为按时间增加普通刷新量、固定精英时点与 240 秒终局 Boss。部分同类敌人的生命与移速随生成时刻增长，不再维持旧版同种敌人基础属性固定的规则。
- 新版使用 3000×2200 的有限战场；普通敌人刷新设有数量阈值，弹丸与掉落集合也有限额。旧版的无限地图与无敌人存活软上限口径没有延续。
- UI 逻辑视口从 640×360 改为 1280×720，重画标题、夜境、HUD 与卡片；角色精灵、部分地砖和音频继续复用已有内部素材，不是全新美术资产。

### 本版未迁移或收缩的功能

- 未接入旧版无限区块流送、多群系生态、世界结构系统，以及可缩放/拖拽的旅行大地图；仅保留有限战场上的简易小地图。
- 未接入 TH01–TH20 可选内容包、清单验证/冻结契约、按包隔离的角色与符卡目录；旧数据仍留在仓库，不进入新版运行。
- 可选角色收敛到灵梦与魔理沙，Boss 收敛到一名雾中来客；旧目录登记的 132 个角色身份与 51 个符卡定义没有迁移。旧登记数量也不代表所有内容均已完成验收。
- 未接入原作符卡来源映射和逐作弹型/时序演出、六类图鉴、详细构筑状态页、局外货币与永久修行。
- 击破最终 Boss 后直接结算，不再提供保留构筑继续无尽游历。
- 设置简化为音乐开关、音效开关、减少震屏和 F11 全屏；双键位重绑、音量滑块、分辨率、VSync 与帧率上限设置未迁移。
- 新版菜单暂未接入旧版游戏内版本日志页和性能诊断界面。仓库 CHANGELOG.md 的历史记录完整保留；本轮另外为成品目录附上完整日志文件。
- 自动射击的提前量改为距离/弹速估计，未迁移旧版按目标速度求最早有效拦截解的算法；不能把这一简化描述为预判精度升级。

### 保留能力与验收边界

- 自动攻击、经验拾取、升级三选一、分阶段 Boss、暂停、胜败结算、重开、音频和单文件 Windows EXE 都是旧版已有能力，本版重写了实现，并非首次新增。
- 新运行时使用独立 .NET 战斗状态与 Godot 显示层，替代旧 ECS/OOP 与内容包架构；旧代码保留但不参与编译。实现更集中不等于内容更多或性能一定更好。
- 19 项新核心回归与新界面/存档流程通过；这些测试不覆盖旧版已移除的系统，不能替代旧版的集成测试覆盖，也不能证明游戏已经更好玩。
- 单文件 EXE 已在项目外隔离目录通过启动和实际渲染检查。此轮仅补充文档及成品旁的日志，不覆盖此前导出的 alpha-0.0.6 EXE；该 EXE 内嵌的是导出当时的简版日志，也没有游戏内日志入口。

## alpha-0.0.5

### 版本定位

- 当前仍处于 `alpha` 开发阶段；大版本和发布序号保持 `0.0`，优化序号推进到 `5`。
- 本版本集中交付自适应五分钟战斗节奏、可持续增长的敌群供给、重新校准的升级选择，以及彼此独立的普通弹与中心弹幕构筑。

### 动态难度与敌群

- 有限流程不再按固定时间自动换档；系统持续统计最近三十秒滑动窗口中的击破数 `K` 与实际刷新数 `S`，观察完整且 `K >= ceil(0.90 * S)` 时才进入下一压力档。
- 换档判断逐帧滑动，不使用互不相交的三十秒分段，也没有超时强制换档、自动清场或敌人存活软上限。
- 八档总刷新率提高为每秒 `2.40 / 3.15 / 4.05 / 5.10 / 6.15 / 7.20 / 8.25 / 9.30`，确保玩家形成割草能力后仍有足够敌群承接成长。
- 敌群组成从 `100/0/0/0` 平缓过渡到 `40/30/22/8` 的普通、强敌、精锐和头目配比；压力主要来自数量，不会在换档时偷偷提高同一种怪物的基础属性。
- F3 调试层同步显示压力档、综合难度、每秒刷新、敌群占比和滑动窗口 `K/S`，用于直接核对换档与供给是否一致。

### 升级与自机弹幕

- 压力阶段不再自动赠送玩家弹数或阵形；普通弹和中心弹幕的数量、表现形式全部回到升级选择中成长。
- 普通弹始终对最近目标的预测交点发射，可保持扇面齐射或选择两翼收束；目标不可追及时才回退当前坐标。
- 中心弹幕以自机为圆心向外辐射，并可通过阵式形成二重、三重或同修后的四重螺旋，不再冒充锁敌弹或以无命中的轮转弹替代输出。
- 两类弹丸共享伤害、射击间隔和弹速，数量与阵形则独立升级；任何阵形都不会凭空增加实际弹丸数量。
- 普通升级继续使用 `当前重数/等级上限`，满重后退出候选池；当前不设置互斥，三选一保留精进、成势与补缺，并限制每轮最多一张奥义。

### Boss 与符卡演出

- 角色 Boss 基础生命统一提高到原始档案的六倍，接触伤害和移动速度保持由种类定义，不随压力档额外成长。
- Boss 仍遵守最近目标、普通弹碰撞和原有战斗规则；不会获得专用穿透或保留火力，玩家需要在 Boss 出现前靠构筑自然形成清杂能力。
- 本体与东方红魔乡 Boss 首批接入所属符卡的弹幕图集、形状和阶段演出；奥义弹丸也从原作 BulletAtlas 的不同行列取样，不再统一显示同一种阴阳玉。

### 内部发行边界

- Windows 正式产物命名为 `TouhouWuxiaSurvivor_alpha-0.0.5.exe`，Windows 文件版本映射为 `0.0.5.0`，PCK 继续内嵌。
- 原作素材继续只用于内部验证；本版本不代表这些素材已经取得公开再分发授权。

## alpha-0.0.4

### 版本定位

- 当前仍处于 `alpha` 开发阶段；大版本和发布序号保持 `0.0`，优化序号推进到 `4`。
- 本版本集中修正移动敌人的普通自动射击命中率，不改变索敌优先级、伤害、射速或弹幕数量。

### 自动战斗

- 普通自动射击不再只瞄准敌人当前坐标；现在按目标权威速度、玩家弹实际速度、枪口出生偏移和两秒寿命解析最早拦截方向。
- 正式 ECS 与旧节点兼容链共用同一预判公式；目标不可追及、距离超出弹丸寿命或输入异常时才安全回退直瞄。
- 每轮齐射重新读取目标运动状态，横移、绕射和远离目标改变方向后会在下一轮更新提前量。

### 单局节奏与奥义 HUD

- 本体有限流程收敛为约五分钟：45、90、150、210 秒逐步展开敌群与弹幕，270 秒进入异变核心，目标在约 300 秒完成 Boss 战与结算。
- 顶部阶段条、敌人解锁、刷怪批次、玩家弹幕和角色 Boss 共享同一套新里程碑；通关后的无尽选择保持不变。
- 每张已悟奥义在底部状态栏右上方显示独立文字印章；冷却遮罩的下边缘随周期向上收缩，就绪后完全退去。
- 主攻与护持图标保持固定分组和目录顺序，不会因剩余时间变化而交换位置；被动条件尚未满足时使用金色待机边框。

### 内部发行边界

- Windows 正式产物命名为 `TouhouWuxiaSurvivor_alpha-0.0.4.exe`，Windows 文件版本映射为 `0.0.4.0`，PCK 继续内嵌。
- 原作素材继续只用于内部验证；本版本不代表这些素材已经取得公开再分发授权。

## alpha-0.0.3

### 版本定位

- 当前仍处于 `alpha` 开发阶段；大版本和发布序号保持 `0.0`，优化序号推进到 `3`。
- 本版本集中交付高刷新率画面连贯性、游戏内版本日志和新的内嵌 PCK Windows 构建。

### 单局节奏

- 本体流程统一为十五分钟五阶段：异变初兆、妖气初现、百鬼渐行、弹幕成形与结界震荡；敌人解锁、刷怪批量和玩家自动弹幕共享同一组里程碑。
- 顶部新增紧凑分段异变进度带与短暂阶段提示，底部生命、灵息和战斗状态栏尺寸保持不变。
- 角色 Boss 首次出现时间从旧两分钟调整到十五分钟；首次击破后可选择“平定异变”成功结算，或保留当前构筑进入无尽游历。

### 升级选择

- 成熟构筑的三选一按亲和自然组成“顺势、补全、另辟”三类职责；缺少合法候选时才由普通机缘补位，内容包仍只横向扩充身份。
- 升级卡片显示职责、类型、下一重、亲和与重数轨迹，特化节点使用独立定型标记，不再把全部信息挤进两行按钮文字。

### 界面与版本日志

- 主菜单新增“版本日志”入口，直接读取随包 `CHANGELOG.md`，不在代码中维护第二份重复文案。
- 日志页采用固定版本索引与正文两栏，支持查看当前及历史版本、正文区域滚动和 `Esc` 返回。
- 日志页保持在 `640 x 360` 逻辑视口内，不使用可拖动分界线；版本、分节和改动数量均从结构化解析结果生成。
- 正式与诊断导出预设显式收录日志源，并由版本策略测试锁定当前标题、文件名和 Windows 数字版本。

### 画面连贯性

- 启用 Godot 物理插值，并为 ECS 敌人、弹幕和灵息保存前后固定步位置；渲染帧按插值比例绘制，避免无上限帧率下重复显示 60 Hz 物理位置。
- 原点重定位统一在固定物理步执行，同时平移 ECS 轨迹两端并重置 Godot 插值，避免跨区块时出现单帧回拉。
- 视频设置页与 F3 调试信息显示软件帧率上限和 VSync 的实际状态，区分“无限制”与持久化的有限档位。
- 这些改动处理的是帧时间与画面运动连贯性，不把无上限 FPS 数字本身当作稳定性的唯一结论。

### 内部发行边界

- Windows 正式产物命名为 `TouhouWuxiaSurvivor_alpha-0.0.3.exe`，Windows 文件版本映射为 `0.0.3.0`，PCK 继续内嵌。
- 原作素材继续只用于内部验证；本版本不代表这些素材已经取得公开再分发授权。

## alpha-0.0.2

### 版本定位

- 当前仍处于 `alpha` 开发阶段；大版本和发布序号保持 `0.0`，优化序号推进到 `2`。

### 数值与技能策划

- 统一普通攻击为 `10 / 0.28 秒` 基准，并以六种手工角色定位分配生命、移动、攻势、普攻节奏和奥义资质，移除摘要随机数值。
- 重做六路有限武学、互斥行为特化及六路平方根无尽延续；强攻、速射、身法、阵法、守御和聚灵路线均有正式运行消费者。
- 三选一改为构筑亲和加权、无放回抽取、固定探索位及每轮最多一张奥义；启用更多作品只扩充横向候选，不改变奥义总权重和 `4 主攻 + 2 护持`容量。
- 本体以灵梦与魔理沙的 6 张常驻奥义独立填满 `4 主攻 + 2 护持`；二十个作品包各提供 2 张同预算替代项，完整目录共 46 张。
- 46 张奥义全部重新校准，保留独立定时或战况被动触发且不消耗灵力；新增环身、扇面、贯线、圆环、背袭五类数据驱动几何。
- 贯穿次击改为整轮首击预算的 30%，散华七弹与破甲双目标按相同总伤校准；状态页显示齐射总伤、弹数、单弹区间及贯穿次伤。
- 修复灵息奖励时间倍率双算；时间轴模拟改为消费正式批次、间隔、存活上限和击破吞吐，并覆盖至 120 分钟。
- E 状态页增加只读交互构筑图，展示角色定位、节点前置与互斥、六维亲和、六路无尽、奥义容量和实时倍率。
- 本版本面向内部完整玩法验证，集中整合无限世界地理、横向构筑、奥义运转与 ECS 性能改造。
- 原作素材仍仅用于内部验证；`alpha-0.0.2` 可执行文件不得视为已完成公开再分发授权的正式发行版。

### 地图、地区与结构

- 无限世界采用连续抖动 Voronoi 宏域，同作地区形成外围、内部、核心的稳定空间关系，并由区块保存正式群系语义。
- 结构改为数据定义的间距、分离、概率、盐值、占地、稀有度、朝向和变体；分层模板可跨区块连续落地。
- 旅行地图按探索视野揭示群系与结构，支持群系身份色、八档语义缩放、拖动浏览和保留上次查看位置。
- 首次进入仅同步生成核心 `3 x 3` 区块，外围按帧预算补齐，降低进入无限世界时的主线程尖峰。

### 奥义运转与属性缩放

- 奥义统一不消耗灵力；各张已悟符卡既可按独立周天定时主动运转，也可由明确战况条件被动触发，不增加玩家操作按键。
- 新悟的定时奥义从完整周期开始；攻速或构筑变化会按已完成进度平滑换算剩余时间，避免重置或无故抢跑。
- 内容包只保存倍率，最终周期、伤害、范围、弹速、目标数、护身时间与弹道参数均由角色基础属性和本局构筑实时求值。
- 计时、属性提供、解锁来源和效果执行通过窄接口组合，世界场景只负责装配，不再承担奥义规则。

### ECS 性能与诊断

- 玩家弹碰撞采用空间索引，弹幕阵营数量改为常量时间统计，正式绘制剔除视口外实体并保留边缘弹幕余量。
- 区块补齐、HUD 与调试信息均按独立预算更新，避免地图流送和不可见调试文本持续挤占战斗帧时间。
- Windows Release 与诊断变体共享 `alpha-0.0.2` 游戏版本；诊断版保留独立目录、控制台、调试符号和结构化性能日志。
- Release 托管程序集不再编译测试与素材构建工具；资源过滤同时隔离旧示例资产和根目录诊断残留。
- Windows 可执行文件图标与游戏内纹理统一采用最近邻插值，避免像素素材在系统缩放时被平滑。
- Windows 默认使用 OpenGL Compatibility；D3D12 仍作为诊断对照入口，用于区分渲染后端异常与 ECS 实体规模压力。
- 启用 Godot 节点物理插值，并为 ECS 敌人、弹幕和吸附灵息保存前后物理位置；渲染帧按插值比例重绘，消除高刷新率下重复显示 60Hz 旧位置造成的顿挫。
- 原点重定位改由固定物理步统一执行，玩家、场景实体和 ECS 轨迹同步清除旧坐标；视频设置页与 F3 同时显示软件帧率上限和 VSync 的实际生效状态。

## alpha-0.0.1

### 版本定位

- 该版本仍处于 `alpha` 开发阶段；大版本和发布序号保持 `0.0`，优化序号由 `0` 推进到 `1`。
- 主要玩法、内容目录和运行时链路已经贯通；本阶段重点转向完整性、性能、平衡与发布验证。
- 该版本仍是内部开发版本，不代表东方原作素材已经取得公开再分发授权。

### 幻想乡与全作内容

- 幻想乡本体和 TH01 至 TH20 全部接入地区、结构、普通敌人、角色、符卡五类增量内容。
- 二十个正作内容包可独立启用、收起并查看详细清单；旧作显示由独立开关控制。
- 无限世界按已启用作品生成对应群系、结构和生态，未启用作品不会向本体泄漏专属内容。
- 图鉴同步覆盖五类内容，提供动态图标、来源筛选、实战数值和长值独占行排版。

### 角色与符卡

- 133 条清单登记规范化为 132 个稳定角色身份，全部可作为自机，也可进入角色 Boss 候选池。
- 每局严格按稳定角色 ID 排除当前自机，避免同一角色同时以玩家与 Boss 身份出现；其他局仍可作为 Boss。
- 全作共接入 42 张自动符卡：TH06 四张，其余 TH01 至 TH05、TH07 至 TH20 各两张。
- TH06 至 TH20 按原作正式符卡登记；没有符卡规则的 TH01 至 TH05 明确标注为旧作攻击意象的武侠化拟制。
- 奥义不消耗灵力，同时支持独立周天定时主动运转和战况条件被动触发；伤害、范围、弹速、目标与护身实时继承角色和本局构筑属性。

### ECS 战斗与 Boss

- 普通敌人使用 ECS 批量执行追踪、绕射、蓄势突进三种 AI，不为高数量实体创建独立行为节点。
- 角色 Boss 独立于普通刷新权重，并按生命阶段切换瞄准扇形、完整环形波、正反交错旋转弹幕。
- 敌方弹丸具有独立阵营、碰撞、伤害与池预算；Boss 击破和存活数量进入正式运行统计。
- 正式敌人、Boss、掉落物、灵息和双方弹幕统一走数据驱动的 ECS 战斗链路。
- 玩家弹碰撞改为 64 像素空间索引，弹幕阵营数量改为常量时间统计；正式绘制会剔除视口外实体，保留边缘弹幕余量。

### 无尽成长与弹幕演进

- 敌人刷新预算、生命、伤害、奖励和总体强度随时间持续增长，不设置玩法意义上的终局上限。
- 单批生成数、刷新间隔、敌人移速和投射物数量只保留明确性能边界，防止极长局破坏帧率与碰撞稳定性。
- 等级需求保持单调增长并采用长整型累计经验；六项有限修行满五重后解锁三项可重复无尽修行。
- 玩家自动攻击由前两分钟单发，逐步过渡到三、五、七发交错扇形，并在后期穿插旋转环形弹幕。
- 普通远程敌人与角色 Boss 共享无尽强度时间轴，后期同时提高射速、弹速、伤害和波次密度。

### 性能诊断与帧率修复

- 修复旧设置可把任意整数直接写入引擎帧率上限的问题；非法 `MaxFps=3` 现在迁移为最近的合法档位 `30`，设置界面与实际引擎值保持一致。
- 新增独立的 Windows 诊断构建变体，不改变 `alpha-0.0.1` 游戏版本，也不覆盖正式构建产物。
- 诊断会话按秒记录真实帧间隔、CPU/GPU 帧时、内存与 GC、区块、敌人池、Boss、双方弹幕和碰撞遍历压力，并保留修复前的帧率上限证据。
- 随包提供 D3D12 与 OpenGL 两套启动入口和中文回传说明，用同一玩法状态区分设置锁帧、渲染后端异常与后期实体规模瓶颈。
- 诊断日志使用 UTF-8 无 BOM 的缓冲 JSONL；写盘或系统指标失败只停用诊断，不中断游戏，普通发行预设不会激活采样。
- 同机 A/B 日志确认 D3D12 在主菜单即发生主线程后端异常，而 OpenGL 在更高战斗负载下正常；Windows 默认改为 OpenGL Compatibility，D3D12 保留作诊断对照。
- 用户回传的 OpenGL 会话在正式游玩阶段约为 298 至 645 FPS；由此排除该机器上的三帧锁帧与 GPU 瓶颈，剩余首进世界尖峰由同步区块初始化处理。

### 横向构筑与无限世界

- 构筑改为刚、巧、疾、阵、御五种横向亲和；亲和只由本局已选内容积累，角色、地区、敌人和作品来源均不修改候选权重。
- 三选一加入无放回加权、固定探索位、多前置、互斥与等预算双分支特化，并修复符卡查询重数导致的异常和永久暂停。
- E 状态面板新增属性/构筑双页；构筑页以四条泳道显示已得路线、亲和、前置、互斥、特化与符卡，可筛选、点选、拖动和缩放，但不会在查看时修改本局选择或候选权重。
- 地区改为连续抖动 Voronoi 宏域，同一作品三个地区形成外围、内部、核心关系；区块保存正式群系语义供场景与地图共享。
- 结构改为逐定义的间距、分离、概率、盐值、占地、稀有度、朝向和变体，并由 16 类分层模板跨区块连续生成。
- 旅行地图只揭示玩家周围的圆形视野，结构靠近后才显示；增加群系身份色、八档语义缩放、拖动后重建并保留上次查看位置。
- 首次进入世界只同步生成 3×3 核心区块，外围按帧预算补齐；常驻 HUD 降到 10 Hz，F3 调试文字仅在可见时以 2 Hz 更新。

### 内部原作素材边界

- 人物、敌人、弹幕、场景预览、道具和音频已接入“东方原作弹幕游戏素材包”，不再依赖旧参考游戏素材。
- 游戏图鉴保留“仅供内部验证、公开前替换”的声明；`alpha-0.0.1` 内部包允许携带这些素材用于本地检查。
- 公开发行前必须在对应资产目录逐项替换并重新核验许可，本项目不宣称内部素材具备公开再分发授权。

## alpha-0.0.0

### 当前状态

- 开发大版本、初步小版本和更新号均从 `0` 开始。
- 当前处于 `alpha` 阶段，玩法、内容和接口仍可能发生不兼容变化。
- 已具备无限世界、内容包选择、自动战斗、地图、图鉴和基础局外成长流程。
- 正式战斗实体已迁移至 ECS，敌人、掉落物、灵息与弹幕不再依赖旧示例节点场景。
- TH01 至 TH20 已纳入逐作内容与素材清单；缺少同作来源的条目会明确登记跨作代用或文字回退。
- 正式敌人、道具、弹幕、地图场景预览与音频均改用东方原作相关素材，旧参考游戏资源已从导出规则隔离。
- 本版本仍是内部开发状态；工作区曾保留一个 2026-08-11 22:06 生成的本地 `alpha-0.0.0` 对应可执行文件。该文件早于最终修复且仍含旧 `item_sheet.png`，不能代表当前源码、公开发布或稳定版本。

### 阶段定义

- `alpha`：核心功能持续开发，允许较大调整。
- `beta`：主要功能基本齐备，重点转向完整性和稳定性。
- `rc`：发布候选版本，只接受阻断发布的问题修复。
- `stable`：达到当前版本线的稳定发布标准。
