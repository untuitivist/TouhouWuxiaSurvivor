# Unselected Local Touchup Experiment

This Aseprite file contains an AI base and limited scripted edits to two ofuda and a shoe. The body is still the AI image.
It is not a full-body layered redraw or an approved improvement. Keep it for inspection; use `art/reference/reimu-wuxia-approved.png` for the new image-editing workflow.
Do not overwrite the existing Aseprite source with a generator.
