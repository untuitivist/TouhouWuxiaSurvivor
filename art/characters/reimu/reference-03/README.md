# 灵梦生成参考 03

状态：仅完成本地 ComfyUI 参考图生成与目视审查，尚未获得用户对人体、脸型或画风的确认；不是像素成品，不是 Aseprite 分层重绘，不接入游戏。

后续反馈：用户指出本稿没有武侠风，并允许直接生成像素参考后再用 Aseprite 优化临摹。本稿仅保留为上一轮对照，不能当成完整设计方向；下一轮同时处理姿态、服装功能结构与像素表现，见 ../reference-04。

## 文件与来源

- reimu-reference_00001_.png：832×1216 的原始生成图，保留 ComfyUI 提示词元数据，不改图、不裁图。
- workflow-api.json：可复现的 ComfyUI API 工作流，正向和负向提示词都在文件内。
- run-01.json：实际运行记录，包含 seed、设备、软件版本、输出文件 SHA-256 和完整工作流。
- model.json：模型的发布仓库、固定 revision、文件大小、SHA-256 与许可入口。
- 模型单独保存在 D:/Comfy-Desktop/ComfyUI-Shared/models/checkpoints/animagine-xl-4.0-opt.safetensors；不放进 Git，不打包进游戏。

用户提供的夜雀食堂人物截图仅供目视观察人体、服装与不同尺度的表现；此次没有上传截图或拿截图像素生成。实际使用的是本地文字生成，模型为 Animagine XL 4.0 OPT。

## 本轮目视审查

可以继续讨论的部分：头颈与肩部连接、身体转向、手臂到袖口的连续关系，以及长裙下两腿和脚的完整轮廓。保留了红白巫女服、发带、深色长发、黄领巾和御币，没有增加剑、盔甲或雷电。

不能照抄的部分：

1. 御币纸垂被生成成过长、拖地且交错的条带，握杆手也需重新明确握持结构；临摹时必须修正。
2. 白袖与衬裙使用大面积亮蓝阴影，朱红也过饱和，不符合已认可启动页的克制暖纸／月夜色调。
3. 头部蝴蝶结和服装装饰仍需取舍；这只是基础巫女造型，不能宣称完成了武侠风主题设计。
4. 图像是高分辨率动漫参考，不是像素画；不能缩图、切块、复制图层后称为 Aseprite 重绘，也不能直接生成战斗帧条。

## 后续 Aseprite 边界

先让用户判断这张参考的人体与脸型方向，再在 Aseprite 另建真正的分层绘制文件。原始生成图保持独立；若置入临摹源文件，只放在明确标记且默认隐藏的参考层。人体辅助、线稿、人物部件、色块与阴影需要实际绘制，不以空白图层数或自动切图代替完成度。

本轮没有新建空白的“多图层成稿”来冒充交付，也没有改动 study-02 的任何源文件。用户手改后的 .aseprite 仍是权威来源，后续不得重跑初稿覆盖。正式源文件做好后必须另列确切位置。

## 复现与检查

从游戏 Git 根目录，用现有 ComfyUI 的 Python 运行：

```cmd
D:\Comfy-Desktop\ComfyUI-Installs\ComfyUI\ComfyUI\.venv\Scripts\python.exe -u tools\comfyui\generate_reimu_reference.py --run-name run-02
```

默认只使用 CPU、8 线程；独立监听 127.0.0.1:8189，禁用自定义节点和 API 节点，独立输出与用户目录，不修改 ComfyUI 核心和其他工作流。运行结束只停止自己启动的服务。相同 run-name 会在启动前拒绝，PNG 使用新的序号，不覆盖已有图片。不同硬件或软件版本下不承诺逐像素相同。

日志位于 artifacts/comfyui-reference/download.log 和 artifacts/comfyui-reference/run-01.log。下载通过现有系统代理断点续传，日志包含总量、已下载量、速度、预计剩余时间；完整 6,938,350,040 字节文件通过发布方 SHA-256 校验后才用于推理。未安装其他模型、依赖或节点。

已检查脚本语法、实际生成、PNG 尺寸和哈希、重复运行防覆盖；这些是技术检查，不代表用户认可美术。art/characters/.gdignore 继续阻止参考进入 Godot 导入，本轮不升版、不导出、不推送、不部署。
