// Canonical stage-independent entry point. The legacy file name remains as a
// compatibility implementation until its removal is separately authorized.
await import("./rebalance_spellcards_gamma.mjs");
