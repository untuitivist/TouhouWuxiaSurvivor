# Touhou Wuxia Survivor

当前版本：`alpha-0.0.5`。运行时版本的唯一来源是 `project.godot` 中的
`application/config/version`；版本规则与变更记录见 [CHANGELOG.md](CHANGELOG.md)。

项目采用“一切皆插件”的工程基调，但玩家侧作品统一称为“内容包”：玩法规则由必选模块提供，
内容包只横向增加敌人、结构、角色、技能和表现。幻想乡本体必须独立完整，东方红魔乡作为
第一份可关闭参考内容包。详细定义与验收顺序见
[插件化内核与内容包设计基线](docs/plugin_first_design.md)。

当前阶段是无限幻想乡生存者玩法 Alpha 内部开发版。幻想乡本体与 TH01 至 TH20 均可按包启用，
132 个规范化角色身份都可作为自机或角色 Boss；本局所选自机会从 Boss 候选中排除。
控制、世界生成、区块流送、ECS 战斗、自动符卡和渲染均为当前项目内的 C# 实现。

## 运行

1. 使用 Godot 4.7.1 的 `.NET` 版本打开本目录的 `project.godot`。
2. 等待 C# 项目编译完成。
3. 按 `F5` 运行主场景。

当前机器也可以直接双击项目根目录的 `run_game.cmd`。

主菜单可开始游戏、查看图鉴与版本日志、调整设置或退出。版本日志直接读取随包的
`CHANGELOG.md`，可在固定版本索引中查看当前及历史改动。游戏内按 `Esc` 或 `P` 打开暂停菜单，
可继续游戏、调整设置、返回主菜单或返回桌面；再次按相同按键会继续游戏。

主菜单和游戏内暂停菜单均可打开“幻想乡图鉴”。图鉴可按本体或 `TH01` 至 `TH20`
筛选，并以地区、结构、敌人、角色和符卡五个分页展示内容；敌人页读取实际生命、移动速度、
出现时间、掉落率和栖息地区，并显示威胁等级、战斗定位、出场阶段和基础击破时间。
图鉴预览不是静态占位图：敌人会播放移动动作，地区会显示行人和环境活动，结构会显示
灯火呼吸与往来人物；角色页说明自机/Boss 互斥规则，奥义页展示独立周期与属性缩放系数。

移动默认支持 `WASD` 和方向键。按 `M` 打开或关闭已探索地图，地图打开时暂停世界。
地图按实际 Tile 显示地形、道路和结构细节；左键拖动，滚轮缩放，方向键平移，
固定标注已探索结构名称；鼠标移到已探索地块时，群系名称会跟随光标显示。
按 `F` 或 `Home` 回到玩家位置，
也可以按 `Esc` 关闭。游戏中默认只在底部显示窄状态栏，包含生存时间、击破数、
当前敌人数和强化。按 `F3` 切换左上角调试信息，其中显示 FPS、种子、绝对 Tile、
Chunk、当前群系、流送队列和本局内容包；调试开关可在按键设置中修改或添加第二键。

敌人在 ECS 中按追踪、绕射和蓄势突进三种批量 AI 行动；角色 Boss 在扇形瞄准、环形波与
正反旋转弹幕三个生命阶段间切换。刷新预算、生命、伤害和奖励随时间无尽增长，实体批量、
移速与弹丸池仅设置性能边界。玩家始终自动索敌射击并自动施展已悟符卡，不设置主动技能键；
普通射击按目标当前速度、弹速和弹丸寿命预判移动目标的拦截方向，无法可靠拦截时回退直瞄；
普通攻击由前期单发逐步过渡到三、五发扇形；散射或结阵特化可扩展为七发，奔雷劲会改为
正反旋转弹阵。六项有限修行满五重后分别解锁六项可重复无尽修行，等级经验采用长局安全的
持续增长曲线。

生命归零后游戏立即暂停并显示死亡弹窗；可直接返回主菜单，或进入本局总结查看生存时间、
击破数、最终群系与坐标、启用内容和世界种子。总结页支持重新开始或返回主菜单，两个操作
都会先清除死亡暂停状态，主菜单不会继承上一局的暂停状态。

Windows 发行版使用 `build_release.cmd` 生成到
`release/TouhouWuxiaSurvivor_alpha-0.0.5.exe`。导出预设采用 x86_64 Mono Release 模板、
内嵌 PCK 和内嵌 .NET 构建产物，发行目录不需要独立 PCK。

当前人物、敌人、弹幕、场景预览、道具和音频优先使用“东方原作弹幕游戏素材包”的内部映射，
仅在缺少可用来源时回退为中文名称。旧 `start-game` 参考资源不进入发行包。原作素材仅用于
内部开发验证，并在游戏图鉴中显示内部素材声明；公开发行前必须在对应资产目录逐项替换并
重新核验授权，本仓库不把这些内部素材声明为可公开再分发资产。

设置保存在 `user://settings.json`，包括主音量、音乐、音效、双键位绑定、窗口模式、
窗口分辨率、垂直同步和帧率上限。地图开关默认仅为 `M`，可以在设置中修改或添加第二键；
按键页可一键恢复全部默认绑定。

## 本体与正作内容包

点击主菜单“开始游戏”会先进入本局内容选择。幻想乡本体始终启用；不勾选任何正作时，
世界只生成本体地区、结构、通用敌人、道具和系统。本体内容层当前标记为已完成：

- 地区：幻想乡原野、博丽神社、人间之里、魔法森林、妖怪之山
- 结构：博丽神社、结界庭院、人间之里、魔法阵遗迹、山间梯田、荒野十字路
- 敌人：野妖精、毛玉、妖虫、阴阳玉、森林精怪、山精、流窜妖怪、夜行妖怪、大妖怪
- 角色与成长：博丽灵梦、雾雨魔理沙、六路有限/无尽修行、六张本体自动奥义与局外通用货币成长
- 系统：无限世界、群系敌人生态、随时间增强刷怪、自动战斗、旅行地图、死亡结算

敌人由实际生成位置的群系决定，森林精怪、山精和流窜妖怪分别属于对应地区；夜行妖怪和
大妖怪在生存时间达到要求后加入所有地区。选择列表中的 `TH01` 至 `TH20` 均已完成第一阶段
运行时接入并可独立勾选：每一作加入三个可生成地区、三个会在地图标注的结构和三类
只在所属地区刷新的特色敌人，共计六十个地区、六十个结构和六十类正作敌人。启用多个作品时，
这些内容会共同分布到无限世界，而不是互相覆盖。

各包清单按地区、结构、敌人、角色和符卡五类登记增量内容。全部清单共规范化为 132 个角色
身份并接入自机与角色 Boss 运行时；本体 6 张常驻奥义可独立填满正式槽位，二十个作品包各增加 2 张平行候选，共 46 张。`迷途竹林`、
`竹林古道`和`竹叶妖`严格归属
`TH08 东方永夜抄`，不勾选该作时不会泄漏进本体世界。

每个整数编号正作都拥有 `content/packs/thXX_slug` 独立目录和 `pack.json`；清单按地区、
结构、敌人、角色和符卡分类列出增量名称。包内保留 `actors`、`world`、`assets` 边界，
小数编号外传暂不归入正作目录。

## 世界尺度

- `1 Godot unit = 1 px`
- `1 Tile = 16 x 16 px`
- `1 Chunk = 32 x 32 Tiles = 512 x 512 px`
- 默认保留玩家周围 `5 x 5 Chunks`

生成结果只取决于世界种子和绝对 Tile 坐标。玩家远离本地原点后按整 Chunk 重定位，
避免 Godot 浮点坐标和 `TileMapLayer` 坐标范围导致精度下降。

## 像素渲染规范

所有 2D 纹理默认使用最近邻过滤，所有 PNG 禁止生成 mipmap，并启用 2D 变换与顶点像素对齐。
新增角色、结构、弹幕、特效和 UI 图片应继承项目默认过滤；不要在场景节点上覆盖为 Linear。
`TexturePolicySmokeTest` 会递归审计运行时场景和纹理资源。

武侠界面使用项目内确定性 RGB 绘制器生成宣纸纹理、卷轴九宫格边框、祥云分隔、朱砂印章、
墨山背景以及动态图鉴精灵表。资产说明和重建命令见 `assets/ui/wuxia/README.md`；敌人曲线、
威胁模型与刷怪节奏见 `docs/enemy_balance.md`；角色、修行、奥义、经验和 0 至 120 分钟
时间轴的统一策划基线见 `docs/combat_balance.md`；插件边界和当前实施顺序见
`docs/plugin_first_design.md`。

## 验证

```bat
dotnet build TouhouWuxiaSurvivor.csproj --configuration Debug
dotnet run --project tools\world_verifier\world_verifier.csproj --configuration Release
```
