# 幻想乡 · 夜境异闻

**东方 × 武侠 × 幸存者**。灵梦以御札、阴阳玉与结界退治；魔理沙以星光与魔炮开路。武侠体现在进退与取舍，不覆盖人物身份。

当前候选版本：`alpha-0.1.8`。灵梦保留基础御札起步与兼修成长；魔理沙使用逐颗四射的多体引力星流、药菇续航与可解锁的追敌魔炮/星光共鸣。中英文设置、原有键盘/触屏操作保留，同一 C# 项目交付 Windows/Web。没有特殊要求时优先使用原作素材；实际发布状态以 `docs/deployment.md` 和版本回执为准。

## 成品交付

网页入口：`https://allinagent.top/TouhouSurvivor/`，手机请横屏。本版发布单线程 C# Web 构建，不再要求跨源隔离或 SharedArrayBuffer；仍需浏览器支持其余引擎能力。小米浏览器、Android/iPhone 真机与移动端帧率尚未验收，不能以桌面触控模拟代替。准确部署状态及来源修订见 `docs/deployment.md`，工具链说明见 `docs/threadless-web.md`。

今后正式发布同时交付自带依赖 EXE 与对应版本的网页更新部署。服务器首次 clone 同一仓库，后续 `git pull --ff-only`；构建产物单独传输、校验并保留回滚版本。部署步骤见 `docs/deployment.md`。

待验收交付入口为 `release/TouhouWuxiaSurvivor_alpha-0.1.8.exe`，不是源码启动脚本；没有通过新构建双端门禁前，不代表该候选已上线。
沿用旧版本的单文件口径，PCK 与 .NET 构建输出内嵌；全部旧版 EXE 保留。
版本来源为 `project.godot`，采用 `stage-major.release.optimization`，Windows 文件版本为 `0.1.8.0`。
导出命令为 `build_release.cmd`，同版本成品已存在时拒绝覆盖。下方脚本启动方式仅用于源码开发。

## 启动

本版 EXE 保留**像素 UI、完整设置和 F3 诊断**，增加批量战斗与渲染优化及新图标。日常改动提交 Git；明确发布时升级版本号、导出自带依赖 EXE 并更新网页，保留历史产物。详情见 `CHANGELOG.md` 的 alpha-0.1.8 部分。

Windows 下双击项目根目录 `run_game.cmd`，或在本目录运行：

```bat
run_game.cmd
```

脚本自动构建新版并启动。构建与运行日志位于 `artifacts/launch-build.log` 和 `artifacts/game.log`。
本机已验证 .NET SDK 8.0.302 和 Godot Mono 4.7.1。Godot 位于其他位置时可先设置：

```bat
set "GODOT_EXE=C:\YourGodot\Godot_console.exe"
run_game.cmd
```

也可以用 Godot **.NET/Mono** 编辑器打开 `project.godot`，构建后按 F6 运行 `game/Main.tscn`，或按 F5 运行项目。

## 怎么玩

下表为默认键位，可在“游戏设置 → 操作”修改，Esc 始终保留安全返回。F11 切换全屏后需在 15 秒内确认，超时自动回退。

| 操作 | 作用 |
| --- | --- |
| WASD / 方向键 | 移动，角色能力自动攻击 |
| Shift | 慢移并显示判定点与擦弹圈；不改变魔理沙四方逐颗散星 |
| Space | 闪身，短暂无敌，约 2.8 秒冷却 |
| 1 / 2 / 3 或鼠标 | 升级时选择专属能力或通用修习 |
| Esc / P | 暂停 / 继续；子页面先返回上层，窗口失焦也会自动暂停 |
| E | 查看属性与构筑；再次按下返回原状态，升级候选不变 |
| F11 | 切换全屏 |
| F3 | 开关只读诊断：性能、坐标、种子、实体与窗口信息；不暂停游戏，可改键 |

- **灵梦**：110 生命，仅基础直射御札起步；阵、阴阳玉与梦想封印需解锁。御札可兼修追踪/爆破，阵可兼修敌群选点/禁锢，玉可兼修有限消弹/蓄力发射。
- **魔理沙**：100 生命，仅星流起步；每 0.18 秒逐颗散星，质量独立抽样、多对多引力、近身持续撕扯。分布和寿命可反复修习，药菇、魔炮与满蓄势强化魔炮需解锁；同投入持续单体 DPS 与灵梦对照校准，不做总质量池或保底行星。
- 角色能力与四类公共修习，E 查看效果与出处；攻击数值与说明使用共享配置。本作按幸存者节奏改编，不是原作逐帧复刻。
- 魔炮先显示瞄准线，0.45 秒后照射；基础方向固定，学习追敌后自动转向最近存活敌人，不跟随玩家移动方向。广域、星光共鸣与限量消弹可兼修。封魔阵留在施放位置，离开不带走。
- 擦弹与退治积累符卡蓄势，解锁对应符卡后满槽自动施放，发动时清弹、短暂无敌并吸取经验。每颗弹只能贡献一次擦弹。
- 站入三处古印各七秒，获得回血与一次悟道。进度保留，遇险可以先退。古印是有价值的可选目标，不是强制门票。
- 第四分钟出现最终 Boss，击破即获胜；计时本身不会导致失败。参考自动流程约四分半，但真人通关时间取决于构筑与走位。
- 本地只保存音画偏好、最佳成绩与通关次数，没有局外数值加成。位置为 Godot `user://rebirth/profile.json`。
- 设置支持总音量、音乐、音效滑块；标题与暂停菜单可按版本查看完整更新记录。显示标题更新，内部项目名保留旧存档命名空间，继续读取此前设置与成绩。

## 新架构

```text
game/
  Main.tscn          唯一运行入口
  core/             固定步长、独立于引擎的战斗与成长
  presentation/     Godot 输入、场景绘制、HUD、菜单、音频与记录
  diagnostics/      自动化导航探针，只在明确诊断入口中使用
tests/rebirth/      编译同一核心源码的独立回归测试
tools/rebirth/      验证与截图脚本
```

主项目只编译 `game/**/*.cs`。旧 `src/`、旧测试、旧内容数据和旧工具仍在原位作为历史参考，不通过适配层参与新版运行。
重写前的完整受版本管理快照为 `d229b36`。仓库外素材和 Git 忽略的文件未加入该提交，仍保留原位。

## 设计与验证

- `docs/rebirth_design.md`：共用游戏设计；魔理沙当前成长见 `docs/marisa_growth.md`。
- `docs/rebirth_validation.md`：测试范围、自动流程数据和仍需真人确认的边界。
- `.NOTE.md`：用户确认的方向与项目定义；旧笔记不再是新版硬约束。

```bat
tools\rebirth\verify.cmd
tools\rebirth\capture.cmd
```

已有 58 项核心回归、真实引擎输入与界面流程、音量偏好与旧记录兼容测试。截图包含两人的御札、封魔阵、星弹、魔炮前摇/照射与构筑，并检查 960×540 窗口。批量渲染的实测与限制见 `docs/performance.md`。
日志和截图在 `artifacts/`，不进入 Git。自动化证明运行与规则符合预期，不能证明游戏已经足够好玩。

## 素材边界

本次保留原作人物、敌人、弹幕、特效、草地、树木、鸟居与音频。标题、UI、石板路、装饰和图标边框在 Aseprite 中制作，分层源文件位于 `art/`；不使用第三类程序生成美术。
这些既有素材**不因此获得公开发行或再分发授权**。本项目不是官方作品；用户授权的 EXE 交付、Git 推送和域名试运行不构成原作素材权利方的授权。
