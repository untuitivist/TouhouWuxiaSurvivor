# 已撤回的未发布美术记录 — 2026-09-09

以下完整保留回退前 `072a2eb` 的未发布更新记录，仅用于追溯；这些变更未正式发布，且已按用户要求从活动游戏中撤回。当前游戏内容恢复 `e902d46` 的正式 `alpha-0.1.6`。记录中的源稿／文档按原路径保存在 `artifacts/rollback-alpha-0.1.6-from-072a2eb/snapshot/`，已跟踪内容也保留在原 Git 提交中。历史正式版本条目继续留在 `CHANGELOG.md`，未改写。

---

- 全量像素美术接入：新增 shrine-v04，共 64 项 Aseprite 源文件与导出，覆盖灵梦／魔理沙完整立绘、四向待机／四帧行走／三帧施法、妖怪、御札／阴阳玉／星光／魔炮／阵、神社建筑、石庭、树木、标题、界面与图标。两位角色各保存 32 帧时间轴及完整设定板。旧素材和全部历史源稿保留，不再仅交单张样板。
- 统一俯视斜视呈现：地面深度压缩至 0.72，角色和建筑保持直立；人物、妖怪、阴阳玉及场景采用同一图集、脚底深度排序和遮挡淡化，左／右分别用独立方向帧。键盘和触屏输入经过投影换算，不改变战斗伤害、成长数值、敌人上限或技能时序。
- 角色选择与图鉴接入完整立绘；九宫格边角修正为不重复铺入中心。标题沿用此前月夜鸟居构图的 Aseprite 绘制笔触，调整为朱红树叶与墨蓝夜色；不以参考图抠图、缩放或减色代替重绘。
- 素材制作与校验：脚本在 Aseprite 中绘制，保留部位图层与隐藏锁定参考层；不是鼠标逐笔手绘，也不声称与参考一模一样或已获用户美术认可。源文件复开、PNG 一致、动画标签／四个不同步态、图集区域和双端打包路径加入检查；新建编辑说明。修复 Aseprite 后续新增帧把前一方向施法标签延长的问题。
- 本轮仍为未发布工作：Windows 与 Web 共用项目，运行时版本仍为 alpha-0.1.6；没有发行导出、推送或服务器部署。以下样板记录为历史过程，早期“未接入／未完成动画”不代表本轮接入状态。


- 灵梦单帧绘制样板（未接入游戏）：依据认可设定板，在 Aseprite 中用新编写的像素簇构建正面姿势，保留 03a／03b 两份源稿；03b 修整脸型、眼位、蝴蝶结、发束亮面与领结，提供 11 个绘制层、隐藏锁定的结构／参考层及同尺寸对照。它是脚本绘制样稿，不是人工鼠标绘制或自动参考复写；美术仍待审查，未完成动画，未更换游戏视角或运行时素材。

- 精灵绘制方法研究（非游戏内容更新）：查阅 SLYNYRD 俯视人体／行走／攻击教程、Saint11、cure 像素簇教程及 Aseprite 官方动画文档，新增逐张临摹、身体结构、像素簇、方向／步态、分层与透明边验收清单。归档两位角色 28 份自动参考复写稿，明确不算已完成重绘、不作为运行时素材；游戏视角、能力、数值、版本和线上部署未变。

- 还原度与魔理沙范围纠正：归档用户提供的魔理沙完整像素设定板；两位角色均须忠实临摹给定图，不自行简化。灵梦正面坐标拼形试稿已否定，仅保留源文件审计，不接入游戏；技术分层校验不等于美术验收，未完成全量重绘。

- 美术制作口径纠正：用户要求最终素材必须在 Aseprite 临摹重绘，生成图只作参考；本轮抠图／减色稿不接入游戏。新增隐藏锁定参考层、按部位绘制的灵梦正面流程试稿及排除参考层的导出检查；尚未获美术认可，未替换当前游戏素材或视角，不代表全游戏重绘完成。

- 局内美术方向确认（仅规范，未替换游戏素材）：归档用户认可的俯视像素神社与灵梦设定页，规划正/侧/背面小精灵、场景和技能的联合样板；明确参考图中的环身御符不采用，环绕对象仍为阴阳玉，御札发射、地面阵与既有成长分支不变。按用户要求暂停新模型下载，保留续传文件和全部历史稿。
- 灵梦参考方向确认：归档用户认可的红白御符武侠设定页，作为后续图生图迭代的固定基准；保留其他生成候选与两份未采用的脚本描补实验，明确它们不是全身 Aseprite 重绘或运行时成品。用户确认图生图与 Aseprite 修整交替迭代，不覆盖原图或手改源文件。
- 灵梦本地生成参考（未接入游戏）：在用户同意后下载并校验 Animagine XL 4.0 OPT，使用既有 ComfyUI 的 CPU 模式生成一张 832×1216 全身参考，保存原图、完整提示词、seed、工作流和来源。目视审查标记御币纸垂异常与配色问题；当前不是像素成品或 Aseprite 分层重绘，待参考方向确认后再临摹，不改旧稿、玩法或版本。
- 人物美术方案再次纠偏：study-02 被用户否定，保留历史源稿，不接入游戏；用户允许 imagegen/ComfyUI 生成参考后用 Aseprite 分层临摹。目前只完成本地工具与模型条件检查，尚未生成新参考或交付新美术。
- 灵梦重绘样板02（未接入游戏）：新增人体结构、服装剪影、上色对照，以及24层可编辑Aseprite人物源文件；左右肢体、鞋袜、袖子与面部部件分层，结构辅助层默认隐藏。原文件位置与手改说明已保存，初稿生成器默认拒绝覆盖已有稿件。仅完成单姿势样板，人体与画风仍待视觉审查，不代表游戏内素材已经更新。
- 人物样板审查：用户否定上一轮人体表现；已对照指定《东方夜雀食堂》页面的灵梦、魔理沙立绘，将旧样板标记为失败对照，不接入游戏。后续先重建人体结构再处理服装；本条仅记录纠偏，尚无新人体稿或运行时修改。
- 人物设计样板（尚未接入游戏）：参考用户提供的偶像恋恋主题化设计方法，使用 Aseprite 绘制非大头比例的灵梦、魔理沙分层单姿势稿与尺寸对照。保留原作身份，克制加入系带和衣摆动态；待确认后再制作战斗动画，不替换现有运行时素材。
- 启动页风格纠偏：按用户提供的旧启动页恢复夜景构图、棕金纸质界面与浅绿主按钮；两位角色重新绘制为修长比例样板，不再采用大头Q版。原作图片仍只作参考，其余美术细化待样板确认。此前整套重绘的技术检查不代表用户认可其画风。
- 视觉规则更新：原作图片仅作绘图参考，当前角色、妖怪、御札、星弹、魔炮、阵纹、场景、标题、UI与图标改为同一月夜像素风的 Aseprite 重绘；保留分层源文件和原图参考库存，不导入原图像素。
- 双端共用新美术目录，导出排除旧参考图片；图鉴预览与来源说明同步更新。音频、人物能力、战斗数值、操作与批量渲染架构不变。本项尚未发布。
